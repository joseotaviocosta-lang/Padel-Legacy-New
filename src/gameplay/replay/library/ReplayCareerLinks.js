export function createCareerReplayLinks(metadata) { if (!metadata?.replay_id) return null; return { replay_id:metadata.replay_id, career_id:metadata.career_id, journal:{label:'Melhores momentos',to:`/replays?replay=${metadata.replay_id}&view=highlights`}, tournament:{label:'Assistir replay',to:`/replays?replay=${metadata.replay_id}`}, timeline:{label:metadata.is_historical?'Marco histórico':'Partida',to:`/replays?replay=${metadata.replay_id}`} }; }
export function highlightReferences(metadata){return (metadata?.highlight_ranges||[]).map((range,index)=>({id:metadata.highlight_ids?.[index]||`highlight-${index}`,replay_id:metadata.replay_id,...range}));}
