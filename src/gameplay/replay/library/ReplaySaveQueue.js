export class ReplaySaveQueue { constructor(library){this.library=library;this.pending=new Map();} enqueue(replay,context,options){const id=replay.replay_id;const promise=this.library.save(replay,context,options).then((result)=>({status:result.saved?'saved':'skipped',...result})).catch((error)=>({status:'failed',error,retry:()=>this.enqueue(replay,context,options)})).finally(()=>this.pending.delete(id));this.pending.set(id,promise);return promise;} }
