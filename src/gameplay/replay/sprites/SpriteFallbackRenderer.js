export function drawFallbackSprite(ctx, player, profile, size = 1) { const radius = 9 * size; ctx.fillStyle = profile.shirt_primary; ctx.strokeStyle = '#fff'; ctx.lineWidth = 2; ctx.beginPath(); ctx.arc(0, -radius, radius, 0, Math.PI * 2); ctx.fill(); ctx.stroke(); ctx.fillStyle = profile.shirt_secondary; ctx.beginPath(); ctx.moveTo(radius, -radius); ctx.lineTo(radius - 6, -radius - 4); ctx.lineTo(radius - 6, -radius + 4); ctx.fill(); }
