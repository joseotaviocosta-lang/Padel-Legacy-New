export class MatchPresentationController { constructor(player, director) { this.player = player; this.director = director; this.replayState = null; this.replayed = new Set(); } onFrame(snapshot, model) { if (this.replayState) { if (snapshot.currentTime >= this.replayState.end) { const resumeAt = this.replayState.resumeAt; const speed = this.replayState.speed; this.replayState = null; this.player.pause(); this.player.setSpeed(speed); this.player.seek(resumeAt); this.player.play(); return 'resume_live'; } return 'replaying'; } if (model.event?.type === 'point_end' && !this.replayed.has(model.event.point_id) && this.director.highlights.shouldReplay(model.event.point_id, this.director.settings.automaticReplays)) { const point = this.director.highlights.points.find((item) => item.pointId === model.event.point_id); if (!point) return null; this.replayed.add(model.event.point_id); this.replayState = { start: point.start, end: point.end, resumeAt: Math.min(snapshot.duration, model.event.t + model.event.duration), speed: snapshot.speed }; this.player.pause(); this.player.setSpeed(.5); this.player.seek(point.start); this.player.play(); return 'start_replay'; } return null; } }
