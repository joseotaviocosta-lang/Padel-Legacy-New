export const CAMERA_RULES = Object.freeze({ minimumCutIntervalMs: 1800, maxCutsPerRally: 3, importantPointZoom: true, smashZoom: true });
export class CameraDirector { constructor(rules = CAMERA_RULES) { this.rules = rules; this.lastCut = -Infinity; this.cuts = 0; this.mode = 'full_court'; } resetRally() { this.cuts = 0; } select(event, time, settings = {}, point = {}) { if (settings.reducedMotion || settings.cameraMotion === false) return 'full_court'; if (!event || time - this.lastCut < this.rules.minimumCutIntervalMs || this.cuts >= this.rules.maxCutsPerRally) return this.mode; let next = this.mode; if (event.type === 'point_start') { this.resetRally(); next = 'full_court'; } else if (event.type === 'serve') next = 'follow_player'; else if (event.data?.shot_type === 'lob') next = 'follow_ball'; else if (event.data?.shot_type === 'smash') next = 'auto'; else if (point.rallyLength >= 10) next = 'follow_ball'; else if (['winner', 'celebration'].includes(event.type)) next = 'auto'; if (next !== this.mode) { this.mode = next; this.lastCut = time; this.cuts += 1; } return this.mode; } isSafe(mode) { return ['full_court', 'follow_ball', 'follow_player', 'auto'].includes(mode); } }
