// `Badge` é o mesmo componente que `StatusBadge`, só com o nome genérico
// pedido pela auditoria (seção 4). Não crie uma segunda implementação —
// StatusBadge já cobre pílulas de status/tom em todo o jogo.
export { StatusBadge as Badge } from './StatusBadge';
