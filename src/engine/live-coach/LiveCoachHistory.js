export function buildLiveCoachReport(state){const decisions=state.liveCoach?.decisions||[],adjustments=state.liveCoach?.adjustments||[];return{matchId:state.replay?.match_id||null,coachId:state.liveCoach?.coach?.id||null,suggestionsReceived:(state.liveCoach?.suggestions||[]).length,suggestionsApplied:decisions.filter(d=>['apply','partial','auto'].includes(d.decision)).length,suggestionsIgnored:decisions.filter(d=>['ignore','maintain'].includes(d.decision)).length,decisions,adjustments,observations:state.liveCoach?.observations||[],disclaimer:'As métricas posteriores descrevem associação temporal e não provam causalidade.'};}
