const specialtyMap={tecnico:['technical','offensive'],estratega:['plan','opponent','positional'],fisico:['physical'],mental:['mental'],motivacional:['mental'],duplas:['partnership','positional']};
const coachLevel=coach=>Number(coach?.level)||({basico:1,regional:2,nacional:3,elite:4,lendario:5}[String(coach?.tier||'').toLowerCase()]||1);
export class CoachSuggestionEngine {
  generate(pattern,{coach,pointNumber,setNumber,gameNumber}){if(!pattern)return null;const areas=specialtyMap[coach?.specialty]||[];const expert=areas.includes(pattern.category);const level=coachLevel(coach);const confidenceScore=Math.max(.15,Math.min(.98,pattern.confidenceScore+(expert?.1:-.08)+(level-2)*.03));return{id:`suggestion-${pointNumber}-${pattern.patternId}`,type:'coach_suggestion',patternId:pattern.patternId,category:pattern.category,observation:pattern.evidence,evidence:{sampleSize:pattern.sampleSize,window:'last_5_points',persistence:pattern.persistence},confidence:confidenceScore>=.78?'high':confidenceScore>=.5?'medium':'low',confidenceScore:Math.round(confidenceScore*100)/100,suggestedAdjustment:pattern.adjustment,expectedImpact:this.impact(pattern.patternId),physicalCost:pattern.category==='physical'?'low':pattern.adjustment?.tacticId==='agressivo'?'high':'medium',risk:pattern.adjustment?.tacticId==='agressivo'?'higher_error_rate':'controlled',priority:pattern.priority,createdAtPoint:pointNumber,createdAtSet:setNumber,createdAtGame:gameNumber,coachId:coach?.id||null,specialtyMatched:expert};}
  impact(id){return({opponent_targeting_player:'Reduzir a pressão no alvo e recuperar a cobertura central.',opponent_more_lobs:'Melhorar a resposta aos lobs sem abandonar a rede cedo demais.',volley_errors:'Diminuir erros imediatos, com menor volume de winners.',critical_energy:'Preservar energia com ritmo e risco menores.',plan_not_executed:'Aproximar a execução do objetivo original.'}[id]||'Ajustar o comportamento nos próximos pontos.');}
}
