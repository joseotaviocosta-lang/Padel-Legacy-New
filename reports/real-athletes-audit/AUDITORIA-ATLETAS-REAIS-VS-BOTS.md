# Auditoria — Atletas reais vs. bots no ecossistema do jogo

**Escopo:** auditoria apenas. Nenhuma lógica de jogo foi alterada nesta rodada — apenas leitura de código, execução do harness permanente (`scripts/audit-real-athletes-simulation.mjs`) e geração deste relatório.

> **Nota de atualização (Fase 0 — instrumentação e baseline):** os números de "5 temporadas em escala de produção" citados abaixo (Resultado C, 47,5% de títulos reais) foram obtidos ANTES de uma correção de determinismo no próprio harness — as entidades procedurais recebiam id aleatório (`Date.now()+Math.random()`), que alimentava o hash de seleção do motor e tornava o resultado exato não-reprodutível execução a execução. Isso já foi corrigido (ids explícitos e estáveis + relógio/Math.random seedados) e verificado (mesma seed → saída idêntica). **A baseline correta e reproduzível está em `docs/baseline-pre-refactor.json` / `docs/baseline-pre-refactor-season-tier.md`** (seed `baseline-v1`): 119/158 títulos (75,3%) 100% reais, 38/158 (24,1%) 100% bots — o diagnóstico qualitativo abaixo (partida fria no ano 1, hierarquia de tiers quebrada, duplas históricas nem sempre pareadas) permanece válido e é, na verdade, reforçado pela nova baseline; só os percentuais exatos de "Resultado C" mudaram.

**Achado-guia (leia isto primeiro):** não existe *um* sistema "atletas reais" no jogo — existem **três universos de dados desconectados** que nunca se comunicam entre si, mais uma lacuna estrutural específica que impede os 24 atletas reais do pool de ranking de formarem dupla (e portanto de competirem) na simulação de fundo do World Tour **logo no início de uma carreira**. O sintoma relatado ("torneios vencidos por duplas de bots anônimos enquanto Coello/Tapia estão só 'no ranking'") foi **reproduzido e quantificado** pelo harness da Fase 2, em escala de produção:

- **Ano 1 de uma carreira nova (o momento em que o jogador forma sua primeira impressão): 20 dos 24 atletas reais não entram em nenhuma chave, e 29 de 30 torneios são vencidos por duplas 100% bots.**
- **Ao longo de 5 anos simulados, o quadro melhora sozinho (por acidente, não por design)** — o sorteio aleatório de parcerias eventualmente pareia todos os 24 reais pelo menos uma vez, e a habilidade deles produz até uma vantagem desproporcional (47,5% dos títulos com 2,4% da população). Isso não é uma correção do jogo — é só o efeito estatístico de esperar tempo suficiente pelo mesmo sorteio aleatório que falha no começo.
- **Bug independente, confirmado na mesma bateria:** um erro de nome de campo (`rank` vs. `ranking`) faz a hierarquia de tiers do calendário (Silver→Crown) não filtrar nada na simulação de fundo — um Crown (o evento "máximo" do jogo) sorteia do mesmo pool indiferenciado que um Silver de entrada.

---

## Regra de método (registrada na Fase 3, aplicável daqui pra frente e retroativamente)

**Nada entra nesta tabela como causa antes de ter número.** Uma hipótese plausível não é um achado — é uma hipótese, e fica marcada como tal até ser medida. Esta auditoria já foi corrigida por essa disciplina várias vezes; listar os casos serve pra lembrar por que a regra existe, não só declará-la:

- **Ordem de iteração** (Fase 0.3, `FASE-0.3-DIAGNOSTICO-MERCADO.md`) — hipótese plausível pro sintoma "0/24" investigada e **testada como causa** — não era. O achado real (um teto de 500 nunca documentado antes) só apareceu depois de descartar a hipótese óbvia.
- **Hash de pareamento** (Fase 1, `FASE-1-RELATORIO.md`, item 1C) — distribuição MEDIDA antes de qualquer correção ser aplicada, não assumida a partir do código.
- **Montagem de campo do achado #22** (Fase 3, item 3F) — hipótese inicial (registrada no achado #23) de que o backstop de elegibilidade fosse coautor do estouro de custo de 38min. Perfilada e **refutada**: 0,2% do tempo. A causa real (persistência/reranking, achado #24) só apareceu depois de medir as três fases separadamente, não de assumir qual delas "parecia" mais cara.

Quando uma hipótese for testada e descartada, ela fica registrada assim mesmo (não apagada) — o valor de "isto foi verificado e não é a causa" é tão real quanto o de um achado positivo, e evita que a mesma hipótese errada volte numa fase futura sem essa memória.

**Segunda regra (registrada na Fase 4.1, achado #26): medição só vale se o instrumento reproduzir a fronteira, não só a função.** A Fase 0.1 já tinha corrigido o harness pra chamar as MESMAS funções que produção chama (`advanceDay` + `processGameStateDay`, dia a dia) — e isso foi tratado, por anos de auditoria, como "harness fiel a produção". Não era, inteiramente: produção envolve essas duas chamadas numa única transação por dia (Mobile M3.7); o harness as chamava soltas. Chamar as funções certas fora da fronteira transacional certa ainda é medir um jogo que não existe — todo número de custo gerado por esse harness (achados #18, #20, #23, #24, o crescimento de 7,5×, o OOM da temporada 2) precisou de reclassificação retroativa (ver Fase 4.1) porque a fidelidade parecia completa e não estava. A regra: fidelidade de harness não é só "chama as funções certas" — é chamar com as mesmas fronteiras de transação, na mesma ordem, com o mesmo agrupamento que produção usa. Uma auditoria de código que confirma "as mesmas funções são chamadas" não é suficiente; é preciso confirmar também "no mesmo agrupamento de escrita".

---

## Os três universos desconectados

| Universo | Onde vive | Quantos | Usado para |
|---|---|---|---|
| **Pool de ranking mundial** | `src/data/worldSeed2025.json` → entidade `AthleteProfile` | 24 atletas, 12 duplas | Página Ranking (Circuito/Race/Duplas), simulação de fundo do World Tour (`WorldTourLifecycle.js`), notícias de resultado |
| **Catálogo de adversários do jogador** | `src/players/realAthletes.js` (10 nomes) + `src/players/athleteGenerator.js` (240 fictícios) → `src/lib/bots.js` (`BOTS_BY_DIFFICULTY`) | 10 "reais" + 240 fictícios = 250 | Adversários do jogador em treino/torneio (`generateTournamentOpponent`, `career.js`), mercado de parceiros do jogador (`getAvailablePartners`) |
| **Camada de nomes (relações/comissão técnica)** | `src/lib/relationships.js` (rivalidades, por string de nome) + `src/lib/coaches.js` (nomes gerados à parte) | N/A | Rivalidades, imprensa das partidas do jogador, comissão técnica |

Esses três universos usam **ids completamente diferentes** para a mesma pessoa real. Exemplo — Arturo Coello:

- Pool de ranking: `athlete_arturo_coello`, `overall_rating: 96`, `world_ranking_points: 13000` (congelado desde a criação da carreira).
- Catálogo de adversários: `fip-2026:arturo-coello`, `overall: 97`, sem nenhum estado persistente de carreira (é um template estático, recriado do zero a cada consulta).

Vencer o "Coello" do catálogo (numa partida do jogador) não move um único ponto do "Coello" do ranking. Os dois nunca se encontram no código.

---

## FASE 0 — Inventário do elenco

### 0.1 — Onde vivem os atletas reais e quantos são

- **`src/data/worldSeed2025.json`** — JSON estático, `schema_version: "3.4.4"`, `snapshot_date: "2025-12-31"`. Contém **24 atletas** (`athletes[]`) e **12 duplas** (`teams[]`), todos com `seed_source: "final_2025"`. Consumido por **`src/lib/saveFoundation.js`** (`ensureWorldSeed2025`), que faz `upsertBy('AthleteProfile', ..., athlete.bot_id, athlete)` para cada um — ou seja, entram no jogo como entidades `AthleteProfile`/`TeamRanking` comuns, indistinguíveis estruturalmente de qualquer bot.
- **`src/players/realAthletes.js`** — um **segundo** conjunto, **10 atletas** (não 24), com nomes/ids/overalls ligeiramente diferentes do primeiro (ex.: "Leo Augsburger" no seed vs. "Leandro Augsburger" aqui; "Mike Yanguas" vs. "Miguel Yanguas"; overall de Coello é 96 no seed e 97 aqui). Anotado como fonte `"FIP ranking, 20 July 2026"` — uma data de referência **posterior** e inconsistente com o snapshot do primeiro arquivo (`2025-12-31`). Consumido por **`src/players/athleteCatalog.js`** (`buildAthleteCatalog`), que o mistura com 240 atletas fictícios (`generateFictionalAthletes`) para formar o catálogo de adversários/parceiros do jogador (`src/lib/bots.js`).

**Nenhum dos dois arquivos referencia o outro.** Não há função de sincronização entre eles.

### 0.2 — Existe uma flag que distinga real de gerado?

**Não há uma flag funcional.** O único indício é `seed_source: "final_2025"` (presente nos 24 atletas + 12 duplas do `worldSeed2025.json`) e o padrão de `bot_id` (`athlete_arturo_coello` para reais vs. `ranking-bot-0025` para procedurais, gerado em `src/lib/rankingPopulation.js:52`). **Confirmado por grep em todo `src/`: `seed_source`/`final_2025` nunca é lido por nenhum consumidor** — só é escrito no JSON e nunca verificado por nenhuma função de elegibilidade, seleção de chave, ou UI. É um campo morto. **Isso já é o achado pedido no item 2: não existe, na prática, nenhuma distinção operacional entre real e bot no pool de ranking.**

### 0.3 — Distribuição de OVR: reais vs. os ~1000 bots

**Reais (`worldSeed2025.json`, 24 atletas):** OVR de **83 a 96** (Coello e Tapia empatados no topo com 96; Gonzalo Alfonso na base com 83). Mediana ≈ 86.

**Bots procedurais (`src/lib/rankingPopulation.js`, `buildSupplementalRankingPopulation`):** gerados para completar até `WORLD_RANKING_TARGET = 1000` atletas, a partir do rank 25 (logo depois dos 24 reais). A fórmula (linha ~63) é:

```js
overall = Math.max(35, Math.min(96, Math.round(96 - Math.pow(absoluteRank / 1000, 0.72) * 57)))
```

Isso é **totalmente desacoplado** da fórmula de pontos (`pointsForRank`, calibrada para não ultrapassar os reais). Calculando a fórmula de OVR para os 976 bots procedurais (ranks 25–1000):

| Faixa de OVR | Nº de bots procedurais |
|---|---|
| OVR ≥ 90 | **25** |
| OVR ≥ 85 | **84** |
| OVR ≥ 80 | **154** |

Ou seja: o **bot ranqueado #25** (imediatamente atrás do 24º e último atleta real) nasce com **OVR 92** — acima de Stupaczuk (91) e Lebrón (91), dois dos 24 reais, e comparável a Galán (94)/Chingotto (93). Isso confirma exatamente a suspeita do item 3: **há bots com OVR competitivo com o topo dos reais**, porque a curva de habilidade (OVR) e a curva de prestígio (pontos) são independentes uma da outra — só a segunda foi calibrada para nunca deixar um bot "ultrapassar" um real.

`circuit_category` (linha ~73 do mesmo arquivo) classifica `absoluteRank <= 100` como `'Premier'` — ou seja, o **tier "Premier" do ranking tem 24 reais + 76 bots = 100 entradas**, com sobreposição real de habilidade entre eles.

### 0.4 — Como os bots são gerados; risco de sorteio de atributos

Dois geradores procedurais totalmente separados, ambos determinísticos (hash de seed, não `Math.random()` — reprodutíveis, mas arbitrários):

- **Pool de ranking** (`rankingPopulation.js`): nome/país/idade/estilo sorteados por hash a partir do rank; ver 0.3 para OVR.
- **Catálogo de adversários do jogador** (`src/players/athleteGenerator.js`, `generateFictionalAthletes`): 240 atletas, 6 "tiers" de 40 cada, OVR de `10+tier*14` a `23+tier*14` — o tier mais alto (últimos 40) vai de **OVR 80 a 93**, quase empatando com Coello/Tapia (96/97).

**Sim, existe risco real de um bot "empatar" ou superar tecnicamente um real** — não por sorte pontual de uma partida, mas porque a **distribuição de origem** dos dois grupos se sobrepõe deliberadamente na faixa alta (isso é necessário para o jogo ter graduação de dificuldade, mas ninguém protege explicitamente o topo para os reais).

### 0.5 — Duplas fixas históricas ou pareamento aleatório?

**Nem uma coisa nem outra, hoje: soltos, sem nunca serem pareados.** O `worldSeed2025.json` registra as 12 duplas históricas (`team_coello_tapia_2025`, etc.) apenas como linhas estáticas da entidade `TeamRanking` — **não** escreve nada no campo que o resto do jogo usa para saber quem está de dupla com quem (`AthleteProfile.ai_partner_id`). Esse campo é usado por:

- `src/game-core/aiPartnershipLifecycle.js` (`aiPartnerId()`, `availableAthletes()`, `ensureCanonicalPartnerships()`) — decide quem é "livre" para ser pareado no mercado de duplas dos bots.
- `src/game-core/circuitLifecycle.js` (`updateTeamRankings`, linha 74) — decide quais duplas aparecem/atualizam no ranking de Duplas.
- `src/gameplay/worldTour/WorldTourLifecycle.js` (`buildCanonicalPairs`) — decide quem entra como par nos torneios simulados de fundo.

Como nenhum atleta real nasce com `ai_partner_id` preenchido, **todos os 24 são tratados como "livres" desde o dia 1**, exatamente como qualquer um dos ~1000 bots — e só podem ser pareados pelo mesmo sorteio mensal aleatório (`formNewPartnerships`, até 8 pares novos por mês, escolhidos por hash entre TODOS os agentes livres). Ver Fase 2 para o efeito quantificado disso.

**Achado extra, fora do escopo perguntado mas descoberto no caminho:** mesmo quando dois reais **acabam** sendo pareados por esse sorteio (acontece, ver Fase 2), o par gerado ganha uma `team_key` **diferente** da linha histórica do seed. `updateTeamRankings` monta a chave como `[athlete.id, partnerId].sort().join('_')` → para Coello/Tapia isso dá `athlete_agustin_tapia_athlete_arturo_coello`, **nunca** `team_coello_tapia_2025` (a chave do seed). Resultado: a linha histórica fica **congelada para sempre** (ninguém nunca mais escreve nela) enquanto uma segunda linha "Coello & Tapia" com pontos zerados nasce e cresce separadamente — duas entradas para a mesma dupla, uma delas eternamente morta.

---

## FASE 1 — Auditoria de elegibilidade e inscrição em torneios

### 1.1 — Quem entra na chave? Duas pipelines totalmente diferentes

**(A) A chave do PRÓPRIO jogador** (torneio que ele disputa): `src/lib/tournamentDraw.js` (`createDrawnRun`) chama `generateTournamentOpponent(tournament, profile, roundIdx, usedIds, teamRank, stage)` — **em `src/lib/career.js:375`** — que sorteia 2 membros do catálogo **`BOTS_BY_DIFFICULTY[diffId]`** (o catálogo do item 0.1-B: 10 reais + 240 fictícios, filtrado por faixa de dificuldade), via hash determinístico por `(torneio, jogador, rodada, tier)`. **Não consulta a entidade `AthleteProfile` em nenhum momento.**

**(B) A chave dos torneios que o jogador NÃO disputa** (simulação de fundo): `src/gameplay/worldTour/WorldTourLifecycle.js` (`resolveCompletedWorldTourEvents`) monta os pares a partir de **`Partnership`** ativas (`buildCanonicalPairs`), cada par "escolhe" entrar num torneio da semana via `chooseTournament` (`src/gameplay/worldTour/TournamentSelectionAI.js`, pontuação real por valor esperado/prestígio/fadiga — não é sorteio puro), e o campo final é os `drawSize` pares com melhor `pairScore` (habilidade + entrosamento + ruído determinístico).

### 1.2 — Existe um filtro que EXCLUA atletas reais?

**Não existe um filtro explícito** ("se for real, pule"). O que existe é uma **omissão estrutural**: a pipeline (B) só considera atletas com `Partnership` ativa, e nada no bootstrap do save popula essa relação para os 24 reais (ver 0.5). O efeito prático é indistinguível de uma exclusão, mas a causa é uma lacuna de inicialização, não uma regra.

### 1.3 — Atletas reais têm calendário/agenda própria?

Não, e a pergunta não se aplica à pipeline (B) do jeito que foi formulada: a decisão de "jogar ou descansar" é por **par** (`chooseTournament`), não por atleta individual — mas como os reais nunca formam par, nunca chegam a essa decisão. Não existe um conceito de "agenda pessoal" separado disso.

### 1.4 — Torneios que o jogador não disputa: simulados de fato ou sorteados?

**Simulados de fato**, com uma pontuação real (`pairScore` = habilidade média do par × 1.8 + forma × 0.25 + energia × 0.12 + entrosamento × 0.08 + ruído hash) — não é um `Math.random()` bruto. O campeão é o par com maior `pairScore` entre os `drawSize` (até 32) pares que entraram naquela semana. **Isso está correto e funcionando como projetado** — o problema não está na simulação em si, está em quem chega a participar dela.

### 1.5 — Gating por tier: bug ou design?

**Existe, e é design correto — mas só na pipeline (A)** (torneio do próprio jogador): `TIER_DIFFICULTY_PATHS` em `src/lib/career.js:331` mapeia cada tier de torneio a um caminho de dificuldade por rodada; só os tiers **Elite** e **Crown** (os dois de topo) alcançam a dificuldade `'lenda'` (onde os 10 "reais" do catálogo vivem), e só nas rodadas finais (semis/final). **Legacy Silver realmente nunca coloca Coello na chave do jogador — isso é o design pretendido, confirmado em `src/gameplay/worldTour/EntryManager.js:56` (`"Legacy Silver aberto a atletas sem ranking"`).**

Existe também um campo `is_development_tournament` (`circuitCatalog.js:235`, `true` para Silver/Gold/Platinum) que parece capturar a mesma intenção de design — mas **nunca é lido em lugar nenhum do código**. É um marcador morto, redundante com a lógica que já funciona em `EntryManager.js`.

**Classificação:** o gating por tier na pipeline do jogador é **funcionando como pretendido**. A ausência completa de reais na pipeline de fundo (B) é **bug** (lacuna de inicialização), não uma consequência do gating por tier.

---

## FASE 2 — Harness de simulação (evidência quantitativa)

**Harness permanente:** `scripts/audit-real-athletes-simulation.mjs` (novo, reexecutável a qualquer momento — `npm run audit:real-athletes-simulation`). Roda a pipeline de PRODUÇÃO real (mesmo `resolveCompletedWorldTourEvents`, `processAiPartnershipMarket`, `buildSeasonTournaments` usados pelo jogo), sem reimplementar nenhuma regra, por N temporadas, sem nenhuma ação de jogador.

### Nota de metodologia (leia antes dos números)

A camada de persistência da carreira (`ActiveCareerAdapter.mutateActiveCareer`) clona e regrava o save inteiro a cada escrita de entidade — em produção isso é imperceptível (uma escrita por ação do jogador), mas comprimir 5 anos de simulação de mundo numa única execução expõe esse custo: **uma temporada em escala de produção (1000 atletas, 500 duplas) leva vários minutos**. Para viabilizar 5 temporadas em tempo hábil, testei em **duas escalas**, e a diferença entre elas acabou sendo, ela própria, um achado:

| Rodada | População procedural | Duração simulada | Onde está salva |
|---|---|---|---|
| **A — escala reduzida** | 220 bots (mesma fórmula de produção, só um corte menor da mesma curva por rank) | 5 temporadas | `summary.reduced-sample-5-seasons.json` |
| **B — escala de produção, 1 temporada** | 970 bots (igual ao jogo real) | 1 temporada | `summary.full-scale-1-season.json` |
| **C — escala de produção, 5 temporadas** | 970 bots (igual ao jogo real) | 5 temporadas | `summary.json` |

### Resultado B — escala de produção (970 bots), 1 temporada — **reproduz o sintoma relatado**

```
Torneios resolvidos (mundo, sem o jogador): 30
Classificação dos campeões: 100%_bots: 29 · 100%_reais: 1   (96,7% dos títulos para bots)
Média de torneios disputados — reais: 0.58 · bots: 20.15
Atletas reais que NUNCA apareceram em nenhuma chave: 20/24
Coello/Tapia juntos: 0% das amostras mensais (11 amostras)
Galán/Chingotto juntos: 0% das amostras mensais
```

**Isto é a reprodução direta e quantificada do bug relatado.** Em população de produção, um atleta real tem, por mês, ~8 vagas de par novo disputadas entre ~994 agentes livres — a chance de qualquer real específico ser sorteado em um ano inteiro é da ordem de **9%**, e a chance de DOIS reais específicos (ex. Coello e Tapia) serem sorteados no mesmo mês E escolherem um ao outro como melhor compatibilidade é próxima de zero. Por isso 20 de 24 nunca jogam, e por isso Coello/Tapia nunca formam a dupla histórica.

### Resultado A — escala reduzida (220 bots), 5 temporadas — mostra o que acontece quando um real É pareado

```
Torneios resolvidos: 158 · Classificação: 100%_reais: 105 (66%) · 100%_bots: 53 (34%)
Média de torneios disputados — reais: 37.54 · bots: 34.57
Atletas reais que NUNCA apareceram em nenhuma chave: 0/24
Coello/Tapia juntos: 80% das amostras mensais (55 amostras)
Galán/Chingotto juntos: 80% das amostras mensais
```

Isolando só o ano 1 (antes da persistência de dupla "acumular vantagem"): **20 bots / 11 reais campeões (35% reais)** — já mais parecido com o resultado B, e convergindo para o cenário B conforme a população cresce.

**Por que os dois resultados parecem contraditórios (e não são):** com menos bots concorrendo pelas mesmas ~8 vagas mensais, um real tem uma chance MUITO maior de ser sorteado logo cedo — e, uma vez pareado, a habilidade real (Coello/Tapia somam 192 de OVR combinado, acima de qualquer par de bots possível nessa amostra) faz o par dominar e se manter estável (o sistema só dissolve pares após ≥120 dias e mediante `chemistry`/resultados ruins — um par forte tende a persistir). Em escala de produção, a mesma mecânica de sorteio existe, mas a proporção de ~40 bots para cada 1 real dilui a chance de um real sequer ENTRAR no sorteio a ponto de praticamente zerá-la. **Ou seja: a habilidade dos reais nunca foi o problema — o acesso deles ao sorteio de parceria é que desaba com a escala.** Isso é consistente e reforça o mesmo diagnóstico (lacuna em `ai_partner_id`), só que visto de dois pontos da curva.

### Resultado C — escala de produção (970 bots), 5 temporadas — o quadro completo

```
Torneios resolvidos: 158 · Classificação: 100%_bots: 82 (51,9%) · 100%_reais: 75 (47,5%) · mista: 1
Média de torneios disputados — reais: 31.5 · bots: 23.1
Atletas reais que NUNCA apareceram em nenhuma chave: 0/24
Coello/Tapia juntos: 16,4% das amostras mensais (55 amostras)
Galán/Chingotto juntos: 92,7% das amostras mensais
Top 20 por temporada — reais: 14/2026, 14/2027, 14/2028, 12/2029, 12/2030
```

**Este resultado, lido ao lado do resultado B (1 temporada), revela o achado mais importante da Fase 2: o bug é de "partida fria" (cold start), não de exclusão permanente.** No primeiro ano de uma carreira nova — exatamente quando um jogador forma sua primeira impressão do jogo — o sorteio mensal de parcerias (Fase 0.5) ainda não teve tempo de ciclar pelos ~994 agentes livres o suficiente para incluir os 24 reais: **20 de 24 nunca jogam no ano 1**. Ao longo de 5 anos (60 meses de sorteio), o mesmo mecanismo aleatório eventualmente pareia todos os 24 pelo menos uma vez — e, uma vez pareados, a habilidade real deles produz um desempenho até desproporcional (47,5% dos títulos com apenas 2,4% da população). **O sintoma relatado é mais grave exatamente no início de uma carreira — que é quando o jogador mais presta atenção ao ranking — e se dilui (sem nunca ser corrigido de propósito) conforme o tempo de jogo passa.**

Um detalhe adicional: Galán/Chingotto ficaram pareados em **92,7%** das amostras (a dupla mais estável do teste, uma vez formada), enquanto Coello/Tapia só em **16,4%** — a diferença não vem de nenhuma regra sobre "quem é mais lendário", só do resultado do algoritmo de compatibilidade (`compatibility()`, baseado em gap de ranking + estilo tático + ambição) aplicado ao sorteio hash daquele mês. **Não há nada no código que reconheça essas duplas como historicamente/narrativamente especiais — o que se vê de "fidelidade" de uma dupla é acidente estatístico, não intenção.**

### Achado adicional confirmado na Fase 2: a hierarquia de tiers não filtra nada na simulação de fundo

A quebra por tier do resultado C mostra uma mistura de reais/bots **quase idêntica em todos os 6 tiers** — inclusive Crown, o tier "máximo" do jogo:

| Tier | 100% bots | 100% reais | mista |
|---|---|---|---|
| Silver (entrada) | 19 | 16 | — |
| Gold | 15 | 15 | — |
| Platinum | 15 | 14 | — |
| Masters | 12 | 12 | 1 |
| Elite | 10 | 10 | — |
| **Crown** (o mais prestigiado) | **11** | **8** | — |

Isso não deveria acontecer: `src/gameplay/worldTour/EntryManager.js` (`evaluateTournamentEntry`) já implementa um sistema de elegibilidade por ranking mínimo bem desenhado (`min_ranking` por tier: Platinum 450, Masters 220, Elite 120, Crown 64 — ver Fase 0). **A causa raiz é um erro de nome de campo:** `WorldTourLifecycle.js:77` (`normalizeAthlete`) grava a posição do atleta no campo `ranking`, mas `EntryManager.js:44` lê `athlete.rank || athlete.teamRank || 0` — **um campo que nunca existe** nesse objeto. Resultado: `rank` é sempre `0` para toda dupla avaliada por `chooseTournament`, `hasRanking` é sempre `false`, e a checagem de elegibilidade (`EntryManager.js:57`) só aprova tiers com `minRanking === 0` (Silver e Gold) — **toda dupla é automaticamente INELEGÍVEL para Platinum, Masters, Elite e Crown**, não importa o quão bem ranqueada realmente seja.

Como `assignments` (o resultado de `chooseTournament`) fica sempre vazio para esses 4 tiers, `resolveCompletedWorldTourEvents` cai sempre no caminho de preenchimento de reserva (`entrants.length < 2`, linha ~172), que **ignora elegibilidade por completo** e simplesmente pega as 2 duplas livres com melhor `pairScore` daquela semana — daí a mistura idêntica entre Crown e Silver. **A hierarquia de prestígio do calendário (`TOURNAMENT_TIER_CONFIG`) é, na prática, decorativa para os torneios que o jogador não disputa.**

### Amostragem do catálogo de adversários do JOGADOR (pool separado, não depende de `ai_partner_id`)

Composição real dos 6 níveis de dificuldade (`BOTS_BY_DIFFICULTY`, `src/lib/bots.js`):

| Dificuldade | Total | Reais | Fictícios |
|---|---|---|---|
| Iniciante | 46 | 0 | 46 |
| Amador | 39 | 0 | 39 |
| Competitivo | 42 | 0 | 42 |
| Avançado | 46 | 0 | 46 |
| Elite | 38 | 0 | 38 |
| **Lenda** | **39** | **10** | **29** |

Só na dificuldade **Lenda** (reservada às rodadas finais de Elite/Crown, ver 1.5) um real pode aparecer — e mesmo lá, é só **25,6%** do pool. Amostragem empírica de 500 sorteios reais de `generateTournamentOpponent` na final de um torneio Crown: **232/500 (46,4%) tiveram pelo menos 1 nome real entre os 2 adversários** (valor teórico calculado independentemente: 45,2% — a proximidade valida a implementação). Ou seja: mesmo na final do maior torneio do jogo, **mais da metade das vezes o jogador enfrenta dois adversários totalmente fictícios**, e a chance de enfrentar DOIS reais ao mesmo tempo é bem menor ainda.

---

## FASE 3 — Auditoria dos sistemas de relação com atletas reais

### 3.1 — Mercado de parceiros / propostas de dupla

**Funciona, mas usa o catálogo errado.** `src/pages/PartnerHub.jsx` chama `getAvailablePartners(profile)` (`src/lib/career.js:308`), que empilha `BOTS_BY_DIFFICULTY` até o tier desbloqueado pelo nível do jogador — o **mesmo catálogo de 10 "reais" + 240 fictícios** usado nos adversários de partida, não o pool de ranking. **Sim, o jogador pode receber proposta de "Coello" ou propor a ele** (uma vez alto nível o suficiente para desbloquear a dificuldade Lenda) — mas esse "Coello" é o do catálogo (`fip-2026:arturo-coello`), sem histórico de carreira persistente, e formar dupla com ele **não afeta em nada** o "Coello" do ranking mundial. Não há barreira explícita de ranking/reputação além do desbloqueio por nível de dificuldade.

### 3.2 — Rivalidades

`src/lib/relationships.js` (`processMatchRelationships`) cria/atualiza rivalidade **por string de nome do adversário**, sem checar origem — qualquer nome que aparece como oponente do jogador (incluindo um dos 10 "reais" do catálogo) pode virar rival mecanicamente do mesmo jeito que um bot fictício. **Não há tratamento especial nem exclusão** — mas, de novo, essa rivalidade vive numa camada (registro por nome) desconectada do pool de ranking; render uma rivalidade contra "Coello" aqui não interage com o "Coello" do ranking.

### 3.3 — Notícias/comunidade

Duas fontes, comportamento diferente:
- Resultados do **catálogo do jogador** (torneio/treino pessoal): sim, aparecem na imprensa (`PressArticle`, `Post`) via `src/game-core/tournamentLifecycle.js`, citando nomes reais do catálogo quando aplicável.
- Resultados do **pool de ranking** (simulação de fundo): `resolveCompletedWorldTourEvents` cria `WorldEvent` de resultado citando o campeão real (`champion.name`) — **funcionaria corretamente para reais SE eles algum dia vencessem** algo, mas como a Fase 2 mostrou que isso é raríssimo em escala de produção, a notícia praticamente nunca menciona um real.
- Marcos de ranking (`circuitLifecycle.js`, "Fulano é o novo número 1") dependem de `ranking_position` mudar — como a posição dos reais está congelada (ver 3.5), esse gatilho nunca dispara para eles.

### 3.4 — Contratos e comissão técnica

**Lacuna confirmada: nenhum real (de nenhum dos dois universos) pode virar treinador.** `src/lib/coaches.js` gera nomes de treinadores com seu próprio gerador (`GENERATED_FIRST_NAMES`/`GENERATED_LAST_NAMES`), totalmente desconectado de `AthleteProfile` ou do catálogo de atletas. Não há nenhuma ponte de "atleta aposentado vira comissão técnica".

### 3.5 — Aposentadoria/progressão etária

**Achado mais severo que a hipótese original do usuário.** A pergunta presumia "os reais envelhecem e saem, substituídos por bots" — a realidade é **pior**: os 24 reais **nunca envelhecem, nunca se aposentam, e ficam ali para sempre**. `athleteAgeAt` (`src/game-core/livingCircuitRules.js:34`) só calcula idade dinamicamente se o atleta tiver `birth_date`/`date_of_birth` — **nenhum atleta do pool de ranking (real ou bot procedural) tem esse campo**; o fallback é `Math.max(16, Math.round(Number(athlete.age) || 24))`, ou seja, a idade **estática do seed, para sempre**. Nenhum código em todo `src/` incrementa `AthleteProfile.age`. `isAthleteRetired` só olha `retired`/`career_status`/`market_status` — todos setados como "ativo" no seed e nunca reavaliados para essas entidades. **Uma carreira de qualquer duração termina inteiramente entre bots não porque os reais se aposentaram, mas porque eles nunca chegaram a competir para começo de conversa — e, se algum dia chegassem, ficariam competindo eternamente aos 23 anos.**

---

## Classificação consolidada

| # | Achado | Classificação |
|---|---|---|
| 1 | 3 universos de "atletas reais" desconectados (ranking / catálogo do jogador / nomes de relações) | **Lacuna de design** (arquitetural, provavelmente não intencional na forma atual) |
| 2 | `seed_source`/`bot_id` nunca usados como flag real-vs-bot | **Lacuna de design** |
| 3 | Bots procedurais com OVR sobreposto ao topo dos reais (25 bots ≥ OVR 90) | **Lacuna de design** (curva de OVR desacoplada da curva de pontos) |
| 4 | Reais nunca recebem `ai_partner_id` do seed → nunca entram em `Partnership` → nunca entram na simulação de fundo do World Tour | **Bug** (causa raiz confirmada e quantificada) |
| 5 | Duplas históricas do seed (`team_coello_tapia_2025`) ficam congeladas mesmo se a dupla se formar de novo por sorteio, sob uma `team_key` diferente | **Bug** (causa raiz mais profunda: ver #17) |
| 6 | Torneios de fundo (jogador não participa) são simulados por pontuação real, não sorteio puro | **Funcionando como pretendido** |
| 7 | Gating por tier no torneio do JOGADOR (reais só em Elite/Crown, rodadas finais) | **Funcionando como pretendido** |
| 8 | `is_development_tournament` nunca lido | **Lacuna de design** (código morto, redundante) |
| 9 | Catálogo de adversários do jogador dilui os 10 reais entre 240 fictícios mesmo na dificuldade máxima (25,6% do pool "Lenda") | **Lacuna de design** |
| 10 | Mercado de parceiros do jogador alcança os "reais" do catálogo (sem barreira além do nível) | **Funcionando como pretendido** (mas usa o universo errado — não conecta ao ranking) |
| 11 | Rivalidades funcionam por nome, sem discriminar origem | **Funcionando como pretendido** (mas mesma desconexão do item 10) |
| 12 | Nenhum real pode virar comissão técnica | **Lacuna de design** |
| 13 | Reais nunca envelhecem/aposentam (idade e status congelados no seed) | **Bug** |
| 14 | Ranking/posição dos reais nunca se move fora da simulação de fundo (que eles não alcançam) | **Consequência direta do #4** |
| 15 | O #4 é pior no ano 1 (20/24 reais nunca jogam) e se dilui sozinho ao longo de anos, por acidente estatístico do sorteio, não por design | **Bug** (agrava o #4: o dano é maior justamente na primeira impressão do jogador) |
| 16 | `normalizeAthlete` (WorldTourLifecycle.js) grava a posição em `ranking`, mas `EntryManager.js` lê `rank`/`teamRank` — toda dupla fica sempre "sem ranking" e inelegível para Platinum/Masters/Elite/Crown na simulação de fundo, que cai sempre no preenchimento de reserva sem checar elegibilidade | **Bug** (a hierarquia de tiers dos torneios que o jogador não disputa é decorativa) |
| 16b | **Fase 3, item 3A/3B — bug de config, independente do #22 abaixo (mesmo sintoma: chaves incompletas, causas diferentes).** `WorldTourLifecycle.js:resolveCompletedWorldTourEvents` calculava `drawSize` lendo `tournament.draw_size` — campo que não existe no schema de `Tournament` (o campo real é `main_draw_size`) — então `Number(tournament.draw_size)` sempre resultava `NaN`, caindo no fallback fixo de 32 (`Math.max(2, Number(tournament.draw_size) || config.mainDrawSize || 16)` tratava incorretamente qualquer tier configurado com chave menor OU maior que 32, sempre normalizando pra 32). Corrigido lendo `tournament.main_draw_size` primeiro. Isolado, este bug sozinho não explica os 40,6% de chaves incompletas medidos antes da Fase 3 — é o achado #22 (gatilho `entrants.length < 2`) que faz isso; os dois foram corrigidos na mesma leva de mudanças (Fase 3, 3A/3B) e são fáceis de confundir porque produzem o mesmo sintoma visível (chave nunca preenche). Registrado como linha própria porque, sem isso, "#16b" ficava só citado dentro da narrativa do #22, sem definição própria — exatamente o tipo de referência órfã que esta auditoria cobra de outros sistemas. | **Bug de config, corrigido na Fase 3 — sintoma idêntico ao #22, causa raiz diferente** |
| 17 | `saveFoundation.js` (caminho de PRODUÇÃO, não só harness) semeava `TeamRanking.player1_id`/`player2_id` com o `bot_id` estático do JSON — que NUNCA corresponde ao `.id` real atribuído por `makeId()` na criação do `AthleteProfile`. Achado originalmente registrado como efeito colateral do #5 (Fase 2, item 2F); promovido a achado próprio aqui porque é mais amplo e mais severo do que "duplica linha quando a dupla se re-forma por sorteio" — mesmo sem NENHUMA dupla nova se formando, as 12 linhas históricas de `TeamRanking` já nasciam com `player1_id`/`player2_id` órfãos, apontando para nenhum `AthleteProfile` real, **em toda carreira criada em produção**. Candidato direto a causa raiz do sintoma original que abriu esta auditoria inteira (reais visíveis no ranking, ausentes do jogo) — uma linha de dupla com ids órfãos não resolve para nenhum atleta jogável. Corrigido na Fase 2 (2F): `saveFoundation.js` agora remapeia para os ids reais pós-criação e deriva `team_key` via `teamKey()` (`src/lib/teamRanking.js`), a mesma função usada por `circuitLifecycle.js` quando o mercado forma/atualiza uma dupla. | **Bug** (raiz do #5, corrigido na Fase 2) |
| 18 | `CareerEntityRepository`/`ActiveCareerAdapter.mutateActiveCareer` faz `structuredClone` do save INTEIRO a cada transação de escrita (`create`/`update`/`upsert`/`bulkCreate`/`bulkUpdate`/`batch`), independente de quantos itens aquela chamada grava — uma escrita de 1 item custa o mesmo clone que uma de 100. Seis sistemas diferentes bateram nesse mesmo teto e foram corrigidos um a um pelo mesmo padrão (acumular patches e gravar tudo numa única chamada por transação lógica), sem que o padrão em si fosse nomeado como achado até agora: **(1)** `dissolvePartnerships` (Fase 1.5) — 2 escritas individuais por dupla/mês; **(2)** `generateProspects` (Fase 2.5) — create do atleta + create do evento, por prospect; **(3)** eventos de lesão em `simulateWorldDay` (Fase 2.5) — 1 create por lesão; **(4)** `persistEvents` (Fase 2.5, `livingWorldEngine.js`) — 1 create por evento contextual; **(5)** `formNewPartnerships` (Fase 2.6) — 3 transações por par (2 `updateAthlete` + 1 `Partnership.upsert`); **(6)** `generateWorldEvents` editorial + boletim semanal (Fase 2.7, `livingWorldEngine.js`) — a MAIOR ocorrência medida até aqui: 50,9% do custo de `livingWorld` inteiro, só porque escrevia incondicionalmente todo dia (medição em [FASE-2.6-RELATORIO.md](../real-athletes-audit/FASE-2.6-RELATORIO.md), item 2). É também parte da explicação do crescimento de custo dentro de uma única temporada (perfilamento da Fase 1.5/2/2.5/2.6): o save cresce ao longo do ano, e toda escrita — em qualquer sistema, corrigido ou não — passa a clonar mais dado, então o MESMO código fica mais lento em dezembro do que em janeiro. **Correção do número (Fase 4.1, achado #27)**: os 7,5× originalmente registrados foram medidos SEM a transação por dia (achado #26) — o harness antigo pagava um clone extra por transação de escrita solta, e esse número de clones por dia TAMBÉM crescia ao longo da temporada (mais parcerias formadas/dissolvidas, mais eventos, mais escritas concorrentes), inflando artificialmente a curva. **Remedido com a transação por dia e `clubs.js` sequenciado, temporada oficial completa (900+100, seed `official-900-100-s1`)**: 596,91ms/dia em janeiro → 1016,95ms/dia em dezembro — **1,70× (pico em outubro, 1135,54ms/dia, 1,90× sobre janeiro)**, não 7,5×. O crescimento real (tamanho do save aumentando) é genuíno e continua existindo — só não é multiplicado pela contagem de clones por dia, que agora é constante (~1/dia).

**Registro para leitura futura (Fase 4.2, achado #27)**: as 6 ocorrências catalogadas acima foram corrigidas com base em números medidos pelo harness ANTES do achado #26 — harness que, por não usar a fronteira transacional de produção, superestimava o custo de clonagem em cada uma delas (o mesmo padrão que inflou o crescimento intra-temporada de 7,5× pra 1,70× real). Isso **não invalida nenhuma das 6 correções** — acumular patches e gravar numa única chamada por transação lógica é estritamente melhor, com ou sem a fronteira certa, e nenhuma delas piora nada. O que fica registrado é mais estreito: o CUSTO QUE MOTIVOU essas correções — a única coisa que os números antigos mediam de verdade — nunca esteve no jogo na magnitude que os números do harness sugeriam. Uma sessão futura lendo essa sequência de "6 sistemas bateram no mesmo teto, um deles custava 50,9% de `livingWorld`" não deve concluir que a camada de persistência do JOGO era um problema urgente — era o instrumento de medição que não refletia produção. Produção, não harness, é o que o jogador sente no botão "Avançar" — mas o que o jogador sentia era sempre uma fração pequena do que os números pré-achado-#26 registravam. **Não corrigido na raiz nesta entrega** (cada ocorrência foi tratada no sintoma, individualmente) — registrado como investigação dedicada, agendada para ANTES da Fase 4 (o ranking rolling de 52 semanas vai adicionar escritas semanais sobre a população inteira — é lá que o custo por clone vira problema de verdade, não só um ruído de perfilamento). **Evidência adicional da urgência (Fase 2.7, item 5)**: a tentativa de regime-check de 5 temporadas (900+100, seed `official-900-100-s1`) travou com `FATAL ERROR: Reached heap limit — JavaScript heap out of memory` durante a temporada 2 — antes do que o "~3 temporadas" já documentado no próprio harness (`scripts/audit-real-athletes-simulation.mjs`, comentário "Fase 0.1, achado C"). A vazão de mercado mais alta da Fase 2.6 (formação/dissolução de dezenas de pares/mês em vez de ~1) e novas coleções que crescem sem poda por design (`AthleteCareerLegacy`, Fase 2.6) aumentaram o volume de dados por temporada o bastante pra antecipar o limite de memória. Por decisão do usuário, o regime-check foi adiado para DEPOIS da correção deste achado, em vez de insistir agora (medir um mundo que a própria correção vai mudar, pagando o preço mais alto justamente na medição mais cara). **Projeção pós-correção (Fase 4.1, achado #27 — projeção, não remedição; o regime-check de 5 temporadas não foi rerodado por instrução explícita)**: o OOM tinha duas causas possíveis, uma corrigida e uma não. A corrigida: churn de alocação — 89% menos clones/dia (achado #26) significa 89% menos cópias completas do save sendo alocadas e descartadas por dia, o que reduz diretamente a pressão de GC que provavelmente era o gatilho PRÓXIMO do heap limit (muitas alocações grandes e temporárias, mais rápido do que o V8 conseguia coletar). Isso torna um OOM disparado por RITMO de clonagem bem menos provável na mesma temporada 2. A não corrigida: crescimento genuíno de dados sem poda (`AthleteCareerLegacy` e outras coleções citadas na Fase 2.6/2.7) continua existindo — reduzir a FREQUÊNCIA de clone não reduz o TAMANHO do que eventualmente precisa ser clonado; se o volume de dados por temporada sozinho já se aproximava do limite, o OOM pode reaparecer mais tarde (temporada 3, 4 ou 5) em vez de desaparecer. **Não confirmado por medição — projeção baseada no mecanismo.** Rodar o regime-check de verdade é o próximo passo natural agora que a fidelidade do harness está corrigida, mas fica pra quando o usuário decidir que vale o custo. **Registrado (Fase 2.9, item 4, não investigado ainda)**: `AthleteProfile.ranking_history` (array truncado em 51 semanas, `circuitLifecycle.js`) custa ~5-8KB/atleta no pico (~5-8MB no total da população) — benigno isolado, mas vive DENTRO do mesmo documento de save que é clonado a cada escrita (este achado). Uma vez que o histórico de todo atleta esteja cheio, isso sozinho aproximadamente DOBRA o custo de toda transação futura. Entra como pergunta de entrada pra quando este achado for investigado: esse histórico precisa viver no MESMO documento que fica no caminho quente de escrita, ou poderia viver numa coleção separada, fora dele?

**Investigação aberta na Fase 3 (fechamento), item 3 do pedido "Fechar a Fase 3, depois abrir o #18" — diagnóstico e proposta, SEM implementação:**

**3.1 (o reranking do achado #24 é redundante com o passe semanal de `processWorldCircuit`?) — Não é redundante, mas está superdimensionado.** Comparando campo a campo: `processWorldCircuit` (`circuitLifecycle.js:126`, semanal, `localGame.batch` — 1 clone pra população inteira) escreve 11 campos recomputados de verdade; o bloco `reranked` (`WorldTourLifecycle.js:365-369`, disparado a cada resolução de torneio, `bulkUpdate` isolado — 1 clone só pra ele) escreve só `{id, world_ranking, ranking_position}`, reordenação sem recálculo de pontos. `world_ranking` está morto — todo consumidor (`livingCircuitRules.js`, `QualifyingManager.js`, `careerStory.js`, `pressData.js`, o próprio `normalizeAthlete`) só lê esse campo como fallback depois de `ranking_position`, que nunca falta pra ninguém já tocado pelo passe semanal. Mas `ranking_position` tem propósito real: `EntryManager.js:39` o usa no `resolveEntryRank` que decide corte de elegibilidade do Circuit Finals (`minRank:8`/"top8") e do Legacy Finals (`minRank:16`/"top16 duplas") — sem reranking imediato, esses cortes ficariam presos à granularidade semanal (até 6 dias de atraso), o que os outros 7 tiers toleram mas esses dois não deveriam. Ressalva que reabre o escopo em vez de fechá-lo: quem se beneficia dessa freshness NUNCA é a própria chamada que escreve — `athletes`/`pairs` são montados uma vez no topo de `resolveCompletedWorldTourEvents` e nunca relidos no loop, então a montagem de campo do achado #22 dentro da MESMA chamada já rodou sobre o snapshot velho. Quem lê o valor fresco é uma chamada futura (outro dia) que fizer `AthleteProfile.list(...)` de novo. Veredito: manter o campo, não remover o bloco — mas ele não precisa ser população inteira × chamada isolada pra cumprir esse papel estreito; isso empurra a solução pro escopo do #18 abaixo, não pra fora dele.

**3.2 (que fração do custo TOTAL da temporada esse bloco específico representa) — pequena: 7,58%. O #18 tem alvos maiores em outro lugar.** Medição de temporada completa (mesma seed `official-900-100-s1`, 900+100, instrumentação de ponto único em `processWorldTourDay`, revertida depois de medir): **174,33s em 365 chamadas** de `processWorldTourDay` (a função inteira — seleção de IA + montagem de campo + as 4 escritas, achado #24), contra **2299,51s (38min19,5s) de tempo real de parede da temporada inteira** (mesmo método de medição do achado #23, timestamp de arquivo Birth vs. Modify) — **7,58% do custo da temporada está em `processWorldTourDay`; 92,42% está em outro lugar** (o resto do tick diário/semanal: `circuitLifecycle`, `aiPartnershipLifecycle`, `worldSimulationLifecycle`, `matchLifecycle`, editorial/`livingWorldEngine`, e o crescimento geral de custo por clone já registrado neste achado). Isso muda a prioridade: batizar as 4 chamadas de `resolveCompletedWorldTourEvents` num só `localGame.batch` (opção A abaixo) reduziria, na melhor hipótese, uma fatia de ~7,5% da temporada — não o grosso do custo. A opção B (ampliar a fronteira da transação pro tick inteiro) ataca o padrão sistemicamente, não só a fatia do world tour, e por isso é a que de fato tira a persistência do caminho crítico — não a A isolada. Achado #18 confirmado como achado de arquitetura real (o padrão se repete em todo sistema que escreve, não só neste), mas a investigação de ONDE especificamente os outros 92,42% se concentram (provavelmente o próprio passe semanal de `circuitLifecycle`, que já é 1 clone só mas da população inteira, ou o efeito de crescimento de save já catalogado) fica como próximo passo — não decidido nem medido nesta rodada, por estar fora do escopo pedido ("não implemente nada nesta etapa").

**3.3 (proposta arquitetural, sem implementação) — por que `mutateActiveCareer` clona o save inteiro**: não é cautela gratuita, é o único mecanismo de atomicidade que existe, porque leituras usam `ensureActiveCareer({cloneResult:false})` — referência VIVA ao estado quente, não cópia (decisão de performance deliberada: "não há motivo pra reler e parsear o mesmo arquivo pra cada card da mesma tela"). O fluxo sem transação ativa (`ActiveCareerAdapter.js:253-276`) clona, muta a cópia, salva, e só DEPOIS troca o ponteiro quente. Sem o clone: (1) um mutator que lança erro no meio deixaria o estado lido por toda a UI parcialmente escrito, sem cópia confirmada pra restaurar; (2) qualquer leitura que rode entre início e fim de uma mutação (há `await` no meio) veria o objeto pela metade, porque leituras pegam referência viva; (3) falha de disco depois de mutação direta deixaria memória e disco divergentes até o próximo restart, que recarrega o disco velho e descarta silenciosamente o que o jogador achou que tinha acontecido — a mesma classe de dessincronia invisível que esta auditoria já caçou várias vezes noutros achados. Achado técnico que muda o cálculo de esforço de qualquer proposta: `mutateActiveCareer` checa `this.activePersistenceTransaction` incondicionalmente, como estado do próprio adapter — qualquer `bulkUpdate`/`batch` aninhado dentro de uma transação aberta entra automaticamente no draft compartilhado, sem precisar ser reescrito pra receber contexto de transação.

**Opções reais, com risco/esforço**: **(A) sétima ocorrência do próprio achado #18** — envolver as 4 chamadas de `resolveCompletedWorldTourEvents` (`Tournament.bulkUpdate`, `AthleteProfile.bulkUpdate` ×2, `WorldEvent.bulkCreate`) num só `localGame.batch([...])`, exatamente o padrão já usado nas outras 6 ocorrências e o que `circuitLifecycle.js:126` já faz pro passe semanal — a assimetria é que o passe semanal já aprendeu essa lição e o por-resolução não. `CareerEntityRepository.batch()` já resolve corretamente duas operações sobre o mesmo id no mesmo batch. Esforço baixo, risco baixo; corta 4 clones/chamada pra 1, não reduz o TAMANHO do clone. **(B) ampliar a fronteira da transação pro tick diário/semanal inteiro**, não por sistema — like a orquestração (`gameStateLifecycle.js`/`livingWorldEngine.js`) abrir uma `withPersistenceTransaction` só, deixando os 6+ sistemas do tick chamarem `bulkUpdate`/`batch` como hoje; pelo achado técnico acima, eles entram automaticamente no draft compartilhado sem reescrita. Esforço baixo-médio, risco baixo (reusa mecanismo já provado) — mas muda comportamento de falha: hoje um sistema 3-de-6 que falha deixa 1-2 já persistidos (commit parcial); nesta opção, uma falha desfaz o dia inteiro. Defendo atomicidade de dia inteiro como mais correta (dia meio-simulado é uma inconsistência pior que dia não avançado), mas é decisão de produto, não só técnica. **(C) cópia estrutural (copy-on-write por coleção)** — copiar raso só `career.entities` e as coleções tocadas, ramos intactos compartilham referência com o snapshot confirmado. Esforço médio-alto: exige auditar todo mutator do app (não só os do `CareerEntityRepository`, já disciplinados) pra garantir que nenhum muta in-place sem copiar antes — um `rows[i].campo = x` reintroduz o bug de estado rasgado, silenciosamente. Resolve o crescimento de 7,5× intratemporada já registrado neste achado, não a multiplicação por chamada — complementar a (B), não substituto. **(D) escrita direta com diário de rollback** — descartada: resolve isolamento de falha mas não de leitor (leituras entre mutação e rollback ainda veriam estado errado), sem vantagem clara sobre (B). **Ordem recomendada de investigação**: A primeiro (fecha a sétima ocorrência, menor risco), B em seguida (mesmo mecanismo, escopo maior — tira a persistência do caminho antes da Fase 4, prazo já citado neste achado), C como item de fundo. D fora.

**`ranking_history`, resposta direta**: busca completa em `src/` encontra exatamente DUAS referências ao campo, as duas em `circuitLifecycle.js` (linha que lê o valor anterior só pra truncar/anexar, linha que escreve o resultado de volta) — nenhuma tela, gráfico ou outro sistema em todo o código lê esse campo. É write-only, autorreferente. Candidato limpo pra sair do documento quente pra uma coleção separada — não há hoje nenhum caminho de leitura que precise dele junto com o resto do `AthleteProfile` no mesmo documento. Observação lateral, fora do escopo de persistência: vale perguntar depois se ainda faz sentido acumular 51 semanas de um histórico que nada consome.

**Fase 4.0 — fechamento (1A/1B medidos, 2A/2B aplicados; decisão arquitetural revista à luz do achado #26):**

- **1A (contagem de transações por dia e por chamador)** — medido com o mesmo mecanismo já existente (`dev/persistenceTransactionProbe.js`, estendido pro caminho solo, instrumentação temporária revertida depois). Sem transação por dia: **9,5 transações solo/dia**, espalhadas por 12 sistemas diferentes (`world` 191, `sem-stage` 138, `livingWorld` 111, `staff` 90, `persist` 90, `circuit` 66, `circuitLife` 56, `athleteIntelligence` 40, `relationships` 28, `aiPartnerships` 23, `notifications` 15, `aiCareerStrategy` 7, `spontaneousPartnerMarket` 5 — amostra de 91 dias, população oficial). **Critério declarado antes de medir**: ≥20-30/dia espalhadas → fronteira ampliada se justifica; concentrado em 2-3 sistemas → batching pontual basta. O número medido (9,5/dia) fica ABAIXO do primeiro patamar, mas a distribuição (12 sistemas, nenhum dominante) descarta claramente o segundo — "batching pontual" viraria 12 correções separadas, o mesmo padrão que este achado já cataloga 6 vezes. **Mas o achado #26 (abaixo desta entrega) tornou a pergunta discutível**: a fronteira ampliada não precisa ser construída — já existe e já roda em produção (`dayAdvanceCoordinator.js`/`calendarLifecycle.js`, mecanismo M3.7), pros dois únicos caminhos reais de avanço de dia. O harness é quem nunca a usou.
- **1B (custo por sistema de topo)** — perfilado com `dev/performanceProbe.js:createStageProfiler`, que já existe em produção e nunca tinha sido passado pro harness. Maior consumidor em ambas as variantes (com/sem transação por dia): `world` (`simulateWorldDay`, ~25% do tempo perfilado) — é custo computacional real, não só persistência; `livingWorld`/`persist`/`staff`/`circuit` seguem em ordem. Com a transação por dia, TODO stage caiu de custo absoluto (~55-65% cada) — confirma que a sobrecarga de clone estava embutida na medição de CADA sistema, não isolada num só.
- **2A (campos mortos)** — aplicado: `world_ranking` removido dos dois pontos de escrita (`WorldTourLifecycle.js:reranked`, `rankingPopulation.js`, seed idêntico ao `ranking_position` desde a origem); `ranking_history` movido pra coleção própria (`AthleteRankingHistory`), preservado (não descartado — intenção futura de gráfico de evolução). Migração de saves existentes: os dois campos entram como `undefined` no `bulkUpdate` semanal que JÁ toca toda a população — zero escrita extra. Testado em [scripts/test-athlete-hot-fields-fase4.mjs](../../scripts/test-athlete-hot-fields-fase4.mjs) (14/14 PASS, migração de histórico legado incluída). Efeito isolado medido: uma linha de `AthleteProfile` com histórico de 51 semanas inline pesa milhares de bytes a mais que a mesma linha sem — múltiplo por ~1000 atletas no pico, no documento clonado a cada escrita.
- **2B (redimensionar o reranking)** — aplicado, com um bug real pego pelo PRÓPRIO teste de equivalência antes de qualquer coisa ir pra produção: a primeira versão escrevia só quem entra no top 50 por pontos NOVOS — deixava quem CAI do top 50 (era #3, virou #55 na rodada) com a `ranking_position` ANTIGA, ainda "elegível" pra Circuit Finals/Legacy Finals. Corrigido pra escrever a UNIÃO de quem está no top 50 agora com quem estava no top 50 antes (o gatilho da queda cai LÁ FORA de qualquer subconjunto ingênuo do topo). Troca-off explícito e assumido: tiers de corte mais largo (Gold:800, Platinum:500, Masters:300, Elite:150, Crown:80) perdem frescor no mesmo dia por este bloco especificamente — caem pra frescor semanal, que já era o padrão do resto da população. Testado em [scripts/test-rerank-topn-equivalence-fase4.mjs](../../scripts/test-rerank-topn-equivalence-fase4.mjs) (9/9 PASS, cenário de queda incluído explicitamente).

**Status revisado**: as 6 ocorrências seguem corrigidas no sintoma; 2A/2B fecham duas frentes independentes de bytes/trabalho evitável no documento quente. A causa raiz (clone-por-transação) continua sem correção arquitetural NOVA — mas o achado #26 mostra que a correção que este achado cogitava construir (opção B, fronteira ampliada) já existe e já roda em produção pros caminhos reais de avanço de dia; o gap está no harness de medição, não na produção. Decisão de investir mais na arquitetura de persistência em si fica pendente até confirmar (fora do escopo desta entrega) se existe algum caminho de escrita em produção que ainda escape da transação por dia — o levantamento desta fase não achou nenhum, mas não foi exaustivo além de `processGameStateDay`. | **Achado de arquitetura** (6 ocorrências corrigidas no sintoma; 2A/2B fecham bytes/trabalho evitável no documento quente; a correção arquitetural mais cotada — fronteira ampliada — já existe em produção, achado #26; urgência de trabalho NOVO na camada de persistência revista para baixa) |
| 19 | Camada editorial (`AMBIENT_WORLD_EVENTS`, `src/lib/world.js`) tem só **3 templates** fixos de ambientação, gerados sem parar (agora 1 a cada 3 dias, Fase 2.7 — antes, incondicionalmente todo dia); `event_type` (`noticia`/`social`) não se encaixa em NENHUMA categoria que `getLivingWorldSnapshot` reconhece (`circuito`/`mercado`/`saude`); e o evento nunca aparece no resumo semanal automático porque nunca tem `tier` (sempre `'normal'`, e `getWeeklyRelevantHighlights` só cita `tier==='destaque'`). Na prática, a camada existe quase só como ruído dentro de um widget de 3 itens (o mais visto do jogo, `CareerHub`). A Fase 2.7 reduziu a CADÊNCIA (tratou o sintoma de custo), não o CONTEÚDO — o problema de fundo (3 templates repetidos sem categorização) continua. **Não é urgente e não deve ser corrigido agora** — registrado pra não sumir do radar: em algum momento essa camada precisa ganhar volume de templates e categorização de verdade, ou ser reformulada/removida. | **Dívida de conteúdo** (não é bug — funciona como escrito, só é pobre demais pro que devia entregar) |
| 20 | `GameStorage.js`: toda gravação de carreira (`writeCareer`→`writeJson`, `backup:true` por padrão — o caminho que QUALQUER mutação de entidade persistida percorre, via `CareerManager.saveCareer`) cria um arquivo de backup com nome ÚNICO (`backupFileName` embute `formatTimestamp()` no próprio nome). `BackupManager.backupFile` tem rotação desenhada corretamente (`maxBackups=3`, mantém só os N mais recentes) — mas a rotação só encontra algo pra rotacionar quando o MESMO caminho de backup é reusado entre chamadas; como o nome muda toda vez, a lista de backups existentes pro caminho atual está sempre vazia, a rotação nunca remove nada, e cada gravação deixa **mais um arquivo permanente do tamanho do save inteiro**. Medido (Fase 2.8, item 1, 6 meses de simulação, sem correção nenhuma): 223→946 arquivos de backup, 16,2MB→173,1MB, com o incremento mensal ACELERANDO (19,8MB→42,2MB/mês) — enquanto os dados de ENTIDADE genuínos (`AthleteProfile`+`WorldEvent`+tudo mais) somam só ~5MB no mesmo período. **Esta é a causa raiz confirmada do `heap out of memory` que travou a tentativa de regime-check da Fase 2.7** — não a vazão de mercado, não `AthleteCareerLegacy`, não nenhuma coleção de entidade (todas ficaram dentro do esperado ou corretamente podadas). Confirmado como bug de produção, não artefato de harness — mesmo caminho de código que qualquer sessão real de jogador usa a cada salvamento; o painel de diagnóstico do próprio jogo (`BetaTools.jsx`, "Backups internos encontrados") já expõe a contagem crescente, sem nenhum botão de limpeza. **Corrigido na Fase 2.9, item 1**: `BackupManager.pruneOldBackups` rotaciona por PADRÃO de nome (lista+filtra+ordena por prefixo, não mais por sufixo numérico sobre um caminho reusado) — preserva o timestamp no nome (a feature "3 backups históricos por data" continua existindo, só a rotação passou a funcionar). Validado por reexecução idêntica do diagnóstico de 6 meses: arquivos de backup ficam **estáveis em 6 (2 arquivos × 3 mantidos: o save da carreira e o índice) em TODOS os 6 meses**, contra 223→946 sem a correção — o crescimento em MB que ainda existe (7,1MB→16,3MB) é só o tamanho do save individual crescendo, não mais contagem de arquivo. Também descobertos e corrigidos DOIS acúmulos irmãos do mesmo formato, no mesmo diretório: (B) `CareerRepository.writeBackup` (botão manual do BetaTools + backup de segurança do `applySafeRepairs`), pasta própria por carreira, nunca tinha rotação nenhuma; (C) o backup automático do PRÓPRIO índice de carreiras (`careers-index.json`, gravado a cada criar/carregar/salvar/excluir QUALQUER carreira — mais frequente que qualquer carreira individual). Instalações já afetadas (quem testou antes da correção) são varridas automaticamente no carregamento da carreira (`CareerManager.loadCareer` → `CareerRepository.pruneAllBackups`, best-effort, não bloqueia o load) e por um botão manual novo em `BetaTools.jsx` ("Limpar backups antigos") — medido numa carreira de teste com acúmulo prévio simulado nos três armazéns: 801 arquivos/1,4MB → 9 arquivos/<0,1MB após um único carregamento. | **Bug, corrigido na Fase 2.9** (distinto do #18 — não é sobre custo de clonar o save, é sobre uma rotação de backup desenhada corretamente que nunca disparava por causa de como o nome do arquivo era gerado) |
| 21 | **Fase 2.9, item 2/3/4** — dois achados relacionados, tratados juntos. **(a) Poda mensal do PRÓPRIO harness contaminava `TeamRanking`**: `scripts/audit-real-athletes-simulation.mjs` apagava TODO `TeamRanking` cujo `.id` não estivesse entre os 27 pares históricos seedados — inclusive pares de bots ATIVOS, formados pelo mercado no mês anterior, todo mês. Investigação (código de produção: `WorldTourLifecycle.js`/`EntryManager.js`) confirmou que elegibilidade de entrada de pares em segundo plano usa campos individuais do atleta (`resolveEntryRank`), nunca `TeamRanking` — e nenhuma métrica deste harness (top-20, título-por-tier, composição de chave) lê `TeamRanking` depois do seed inicial. **`docs/baseline-pre-fase3.json` NÃO estava contaminado** — não foi re-congelado. O critério de poda foi corrigido mesmo assim (mesmo formato do #20: "não está no set histórico" ≠ "dissolvido") — só apaga quando não existe `Partnership` com `status:'ativa'` entre os mesmos dois atletas. **(b) Política de acumulação de produção para `Partnership`/`TeamRanking` dissolvidos** (mesmo padrão "só marca, nunca remove" do #20 antes da correção, ~150 dissoluções bot-bot/ano): decisão implementada — `PartnershipLegacy` (agregar-e-então-remover, mesmo padrão de `AthleteCareerLegacy`/Fase 2.6) só é gravada para parcerias que envolvem o JOGADOR ou um atleta REAL (`is_real`) — nunca bot-bot puro, que já tem registro narrativo via `WorldEvent` e cujo histórico ninguém lê (`getPartnershipHistory` filtra por `profile_id` do jogador). A `Partnership` viva em si NUNCA é apagada na hora da dissolução — grep confirmou `Tournament.champion_partnership_id`/`runner_up_partnership_id` (`WorldTourLifecycle.js`, produção) guardando esse id; apagar na hora quebraria a atribuição de título retroativamente se a dupla campeã se dissolvesse depois. Em vez disso, um soft-delete de verdade: `worldSimulationLifecycle.js:pruneOldDissolvedPartnerships`, mesma carência de 24 meses de `pruneOldRetiredAthletes`, mesmo gatilho mensal. `TeamRanking` da dupla desfeita É apagado imediatamente (decisão de COMPORTAMENTO, não faxina — `team_key` é derivado dos ids ordenados dos dois atletas, então uma dupla que se separa e volta a se formar gera a MESMA chave; apagar significa que ela recomeça do zero em pontos, como no circuito real; confirmado sem risco de referência quebrada — nada no código lê uma linha de `TeamRanking` de dupla dissolvida por id, só por `team_key` "ao vivo" ou em listagens do líder do momento). **Sinalizado para Fase 4**: quando o ranking rolling de 52 semanas entrar, este comportamento (dupla reformada não herda ranking) precisa ser RECONFIRMADO como decisão de design, não redescoberto como bug. Migração idempotente das parcerias já dissolvidas em carreiras existentes (de antes desta mudança) implementada em `getPartnershipHistory`/`migratePartnershipHistory` — sem ela, `PartnerHub` ficaria vazio para tudo que aconteceu antes da correção. `Legacy.jsx` também migrado (lia `Partnership.filter` direto da coleção viva pro mesmo fim, mesmo risco). | **Achado (a): falso-alarme confirmado por investigação, corrigido por princípio mesmo assim. Achado (b): política de acumulação definida e implementada** |
| 22 | **Fase 3, item 3B — "montagem de campo": bug independente, mesmo sintoma do achado sobre `draw_size`/`main_draw_size` (registrado como config incorreta na Fase 3, item 3A/3B), causa raiz DIFERENTE.** `WorldTourLifecycle.js:resolveCompletedWorldTourEvents` só disparava o preenchimento de reserva (backstop de elegibilidade, achado #16/Fase 1A) quando `entrants.length < 2` — ou seja, o backstop só garantia o MÍNIMO pra existir uma partida, nunca tentava completar a chave até `drawSize`. Uma chave de 32 com 5 duplas escolhidas pela IA (`chooseTournament`) ficava com 5 pra sempre, mesmo com dezenas de pares elegíveis sobrando no pool — o backstop em si (achado #16) funcionava bem quando chamado; ele quase nunca ERA chamado. Corrigido: gatilho agora é `entrants.length < drawSize`, com log distinguindo "faltou por corte de elegibilidade" de "faltou por pool genuinamente esgotado" (nunca mascarando qual dos dois aconteceu). **Rastreabilidade do diagnóstico errado que este achado corrige** — a Fase 2.6 atacou o mesmo sintoma (chaves incompletas) com a hipótese "faltam duplas no pool" e triplicou as duplas ativas (115→379); o resultado real mede exatamente o tamanho do erro de diagnóstico: <br>• Antes da Fase 2.6: 46,9% incompletas (causa real ainda não identificada) <br>• Depois da Fase 2.6 (triplicar duplas, hipótese errada): 40,6% incompletas — praticamente nada mudou, porque o pool nunca era consultado <br>• Depois da Fase 3 (achado #22, corrige o gatilho): **0% incompletas** — medido, mesma seed/config do baseline (`official-900-100-s1`, 900+100, 1 temporada), 13/32 (40,6%) → 0/80. Bônus: jogador em #1000 saiu de 13/32 eventos elegíveis (maior intervalo 42 dias) para **40/80 elegíveis, maior intervalo 14 dias** (meta da Fase 3, item 3C.1, era ≥15 com intervalo ≤21 dias — superada em quase 3×). | **Bug, corrigido na Fase 3 — causa raiz real das chaves incompletas, independente do bug de config do #16b** |
| 23 | **Fase 3, item 3F** — a mesma medição de baseline (seed `official-900-100-s1`, 900+100, 1 temporada) que confirmou o achado #22 acima também mediu custo: **38min16s de tempo real** (medido por timestamp de arquivo, não pelo relógio determinístico do harness, que é seedado) — muito além da projeção de "+2-3 min" feita a partir do perfilamento da Fase 2.6 (`processWorldTourDay` = 21,3% de `livingWorld` = 18,2% do total). **Perfilamento por fase (achado #24, abaixo) refuta a hipótese inicial de que o backstop do achado #22 fosse coautor do estouro** — é 99,5% persistência. Fortalece a urgência já registrada no achado #18 (investigação agendada para antes da Fase 4) — não é mais só "provável", agora está medido. **Não investigado a fundo nem otimizado nesta entrega** — decisão do usuário, por instrução explícita ("a decisão de reduzir volume é minha"). | **Achado de custo, não corrigido — decisão de próximo passo pendente do usuário** |
| 24 | **Fase 3, item 3F** — perfilamento por fase de `resolveCompletedWorldTourEvents`/`processWorldTourDay` (instrumentação temporária, revertida após medir — mesmo padrão da Fase 2.6 pro custo do `livingWorld`), amostra de 3 meses (mesma seed/config do achado #23, `DIAG_MAX_MONTHS=3`), decompondo em 3 fases: **(1)** seleção de torneio pela IA (`chooseTournament`, por semana) — 0,06s, **0,3%** do total medido; **(2)** montagem de campo (backstop de elegibilidade do achado #22, `evaluateTournamentEntry` por par por torneio) — 0,03s, **0,2%**; **(3)** persistência (as 4 transações de escrita de `resolveCompletedWorldTourEvents` — `Tournament`/`AthleteProfile`/`WorldEvent` `bulkUpdate`/`bulkCreate`) — 18,18s, **99,5%**. **Resposta direta à pergunta que decidia o próximo passo**: o custo NÃO está na varredura de elegibilidade do achado #22 (que é praticamente grátis) — está concentrado na PERSISTÊNCIA, especificamente no bloco `reranked` (`WorldTourLifecycle.js`, fim de `resolveCompletedWorldTourEvents`): reescreve a população INTEIRA de atletas (13.920 linhas de `AthleteProfile` reescritas em só 14 chamadas na amostra de 3 meses, ~994/chamada — praticamente toda a população de ~1000) **toda vez que QUALQUER torneio pendente resolve**, independente de quantos torneios foram resolvidos naquela chamada — e cada reescrita clona o save inteiro (achado #18). **Correção de enquadramento (mesma sessão, antes de fechar a Fase 3)**: 99,5% NÃO é "o preço da feature" — torneios (as duas fases medidas juntas, seleção de IA + montagem de campo) custam 0,5% do tempo; a Fase 3 poderia ter 300 eventos que a computação continuaria irrelevante. O que custa é reescrever ~1.000 linhas de `AthleteProfile` por chamada, e o gatilho é ORTOGONAL ao volume de torneios — dispararia igual com 32 eventos, só menos vezes. **Achado #18 vira o próximo passo natural** — não porque "mais eventos custam mais" (errado), mas porque o reranking por chamada é uma instância nova e concreta do mesmo padrão de clone-por-transação, com um alvo preciso pra começar a investigação (ver achado #18 e a proposta arquitetural registrada com ele). | **Achado de custo, medido e localizado — decisão de investigar o #18 pendente do usuário** |
| 25 | **Fase 3, item 3D — escala absoluta da premiação, calibração pendente** (curva APROVADA, escala não). `TOURNAMENT_TIER_CONFIG`: moedas = pontos de ranking do campeão × 15, calibrado contra salário de parceiro (~80-200/mês) e saldo inicial (100 moedas) — proposta apresentada e confirmada como aplicada. **Observação registrada, não resolvida**: um Crown a 30.000 moedas paga MAIS DE DEZ ANOS de salário de um parceiro iniciante (30.000 ÷ ~200-250/mês ≈ 10-12,5 anos) — pode esvaziar completamente a tensão econômica da carreira justo na fase em que ela deveria ficar MAIS interessante (fim de carreira, torneios de elite), não menos. A curva de decaimento por rodada (mesma razão [0.6, 0.6, 0.5, 0.5, 0.5] dos pontos de ranking) está correta e não muda; o que precisa de reavaliação é o multiplicador de 15× em si, e só faz sentido reavaliar com dados de uma carreira completa jogada (curva de gastos reais — salários acumulados, compras, contratações — não só o ponto de partida). **Não é urgente e não deve ser corrigido agora** — registrado pra não sumir do radar antes que alguém jogue uma carreira até o fim e sinta o problema na prática. | **Dívida de calibração econômica** (não é bug — a curva está certa, a escala pode não estar) |
| 26 | **Fase 4.0, item 1A — o harness deste relatório NUNCA usou o mecanismo de transação que a produção já usa pro avanço de dia real, e isso infla artificialmente todo número de custo medido pelos achados #23/#24.** Produção (`game-core/dayAdvanceCoordinator.js:advanceCareerDayOnce` e `calendarLifecycle.js:advanceCareerDays`) já envolve `advanceCareerDay` + `processGameStateDay` do MESMO dia numa única `ActiveCareerAdapter.withPersistenceTransaction` — 1 clone do save + 1 escrita física por dia, não importa quantos sistemas rodem dentro (Mobile M3.7, mecanismo já existe e está em produção). **Confirmado por busca exaustiva**: `processGameStateDay` tem exatamente DOIS chamadores em todo `src/` — `calendarLifecycle.js` e `dayAdvanceCoordinator.js` — os dois já envolvidos na transação; todo outro chamador (12 arquivos) é `scripts/*.mjs`, ferramenta de harness/diagnóstico, nunca produção. E `processWorldTourDay` (achado #24) roda DENTRO desse mesmo `processGameStateDay`, via `livingWorldEngine.js:processLivingWorldDay` (stage `livingWorld`) — ou seja, em produção, a reescrita de ranking do achado #24 já entra automaticamente na MESMA transação de todo o resto do dia. `scripts/audit-real-athletes-simulation.mjs` chama os dois passos SOLTOS (`advanceDay` depois `processGameStateDay`, sem transação) — decisão original da Fase 0.1 foi sobre ORDEM/granularidade de simulação (dia a dia, não em lotes, por causa de um teste de paridade que achou 68% dos campeões divergindo), nunca sobre a AUSÊNCIA do commit único; ninguém tinha reparado que os dois problemas são independentes.

**Medido, população oficial (900+100), 3 meses, mesma seed `official-900-100-s1`, mesmos 21 torneios resolvidos nas duas variantes (comparação de verdade, não só de escala):**

| | Sem transação (o que o harness sempre fez) | Com 1 transação/dia (o que a produção já faz) |
|---|---|---|
| Transações SOLO (1 clone cada) | 860 (9,5/dia) | 3 (residual, fora do laço de dias) |
| Mutações JOINED (custo marginal ~0) | 0 | 847 |
| Aberturas de transação | 0 | 90 (≈1,0/dia) |
| Tempo de parede (3 meses) | **3min37s** | **2min16s** |

**Redução de 89% na contagem de clones (860→93) e 38% no tempo de parede (217s→136s) — pela MESMA simulação, mesma seed, mesmo resultado.** Reproduzido numa escala menor antes (60+30 atletas, 1 mês): 282 clones solo/31 dias (9,1/dia) sem transação vs. 1 solo + 31 aberturas/31 dias (1,0/dia) com — mesmo padrão em duas escalas independentes.

**Consequência prática**: os 38min16s da Fase 3 (achado #23) e a fração de 7,58% de `processWorldTourDay` (achado #24) são números REAIS do HARNESS, mas não do que um jogador sente no botão "Avançar" em produção — a produção já paga só 1 clone/dia (M3.7, já implementado), o harness paga um clone por CADA transação de escrita solta dentro do dia. Isso não invalida os achados #22/#24 (as correções de bug e a localização do custo dentro de `resolveCompletedWorldTourEvents` continuam corretas) nem o item 2B deste achado (o reranking ainda vale a pena redimensionar, é trabalho e bytes de I/O evitáveis independente de quem clona o quê) — muda a URGÊNCIA da opção arquitetural B do achado #18 (ampliar a fronteira da transação): ela já existe e já roda em produção pros dois únicos caminhos reais de avanço de dia. O harness precisa da mesma correção de granularidade (envolver seu laço de dias numa transação, matching `dayAdvanceCoordinator.js`) antes de qualquer medição de custo futura ser tratada como representativa do jogador. Ver achado #18 (proposta arquitetural, agora com a urgência revista) e achado #24 (números originais, agora com esta ressalva).

**Medição confirmada, temporada oficial completa (Fase 4.1, depois de `clubs.js` sequenciado — achado #27)**: 900+100 atletas, seed `official-900-100-s1`, harness com transação por dia — **5min57,9s de tempo de parede** (era 38min16s sem transação — 84,4% de redução, ~6,4× mais rápido), mesmos 80 torneios resolvidos, 0/80 chaves incompletas. Ver achado #18 pra correção do número de crescimento intra-temporada (7,5× → 1,70×, medido nesta mesma rodada) e projeção do regime-check de memória. | **Achado de metodologia — o instrumento de medição divergia da produção que deveria estar medindo; produção já tem a mitigação que o achado #18 propunha construir; medição refeita e confirmada nesta fase** |
| 27 | **Fase 4.1 — o motor NÃO é determinístico por seed em produção. `Math.random()` é chamado direto, sem seed, em praticamente todo sistema; o "determinismo" desta auditoria inteira sempre foi um artefato frágil do harness, nunca uma propriedade do jogo.** Descoberto ao investigar por que o harness corrigido (transação por dia, achado #26) não reproduz byte-a-byte o harness antigo com a mesma seed — a mesma comparação divergiu com um wrapper de instrumentação e bateu idêntica com outro, o que é a assinatura de uma condição de corrida sensível a TIMING, não a uma linha única de código. Isso reabriu a pergunta de fundo, e a resposta muda o enquadramento de "bug pontual" para "garantia que nunca existiu": <br><br>**Pergunta 1 — `Math.random()` direto ou PRNG seedado?** Contagem em todo `src/` (produção, não harness): **96 chamadas diretas de `Math.random()` em 33 arquivos** (`padel.js`:12, `marketEngine.js`:12, `fanBase.js`:10, `world.js`:6, `worldEvents.js`:4, `weather.js`:4, `bots.js`:4, `clubs.js`:3, `matchLifecycle.js`:2, entre outros 24 arquivos) — sem NENHUM seeding. Existe, à parte, um PRNG local seedado em exatamente 4 lugares (`QualifyingManager.js`/`MainDrawManager.js` — seeding de chave/qualifying por hash de `torneio+perfil+data`; `rankingPopulation.js`/`simulationHealth.js` — geração de população e relatório de saúde) — um padrão bem desenhado (reprodutível por construção, sem precisar de patch global), mas cobrindo só uma fatia pequena do motor. Os outros 90+ pontos de aleatoriedade (mercado, torcida, clima, bots, clubes, eventos de mundo, partidas, treino, personalidade, parcerias, economia, posts sociais) usam o `Math.random()` cru do runtime. <br><br>**Pergunta 2 — de onde vem o determinismo que a auditoria sempre assumiu?** `installDeterminism` (o patch global `Math.random = mulberry32(seed)`) existe em **12 arquivos, todos em `scripts/*.mjs`** — zero ocorrências em `src/`. **Confirmado, não hipótese**: produção nunca teve NENHUM mecanismo de seed — o determinismo que sustentou toda comparação antes/depois desta auditoria (achados #16b, #20, #22, #23, #24, o crescimento de 7,5×, o regime-check de memória) vinha inteiramente de um patch que só existe fora do jogo, e que — como o item acima mostra — nem sequer é robusto à sua própria concorrência interna. <br><br>**Inventário de escrita concorrente sem ordem garantida** (`Promise.all`/`Promise.allSettled` na cadeia de `advanceDay`/`processGameStateDay`, cruzado com quem toca `Math.random()` ou `mutateActiveCareer`): <br>• **`src/lib/clubs.js:227` (`processAllClubsMonthly`, chamada por `career.js:268` a cada virada de mês — bate exato com o dia em que a divergência apareceu)** — `Promise.all(clubs.map(c => processClubMonthlyUpdate(c, {commit:false})))`: N clubes processados concorrentemente, cada um chamando `generateBotMembers` (`Math.random()` direto) e gravando `ClubMember.bulkCreate`/`.delete()` (`mutateActiveCareer`) — candidata principal, todos os elementos de risco presentes ao mesmo tempo. <br>• `src/lib/livingWorldEngine.js:132` (`persistEvents`) — `Promise.all` de LEITURAS de existência (`safeFilter`), grava tudo depois num `bulkCreate` único, sequencial — sem risco (nenhum `Math.random()`, escrita não concorrente). <br>• `src/game-core/seasonLifecycle.js:187`, `partnerLifecycle.js:79`/`166`, `trainingLifecycle.js:80`/`87` — arrays FIXOS (2-4 itens) de creates de tipos diferentes (HistoryEntry/CareerMessage/PressArticle/FinancialTransaction), específicos do JOGADOR (fim de temporada, contrato de parceria, treino) — não escalam com a população, não chamam `Math.random()` direto (só indiretamente via `makeId()` de cada create), risco bem menor e cosmético (só o sufixo do id pode trocar entre execuções, não os dados). <br>• `annualCareerReportLifecycle.js:18`, `coachLifecycle.js:213`, `fanLifecycle.js:55`, `monthlyCareerReportLifecycle.js:32`, `seasonLifecycle.js:49`, `sponsorLifecycle.js:71`, `tournamentLifecycle.js:25`, `clubs.js:179`, `WorldTourLifecycle.js:151`, `globalMarketLifecycle.js:97` — todos LEITURAS puras em paralelo (`Promise.all` de `.list()`/`.filter()`), sem escrita, sem risco. `getGlobalMarketSnapshot`/`evolveWorldMarket` confirmados FORA da cadeia diária (só sob demanda de tela). <br><br>**O que isso implica pras baselines já congeladas**: nenhum número de CUSTO (clones, tempo de parede, achados #18/#20/#23/#24/#26) muda de validade — não dependem de qual entidade recebe qual sorteio. O que perde garantia é qualquer comparação de RESULTADO ESPORTIVO PRECISO (campeão exato, pontos exatos, quem pareou com quem) entre duas execuções — inclusive a própria baseline `docs/baseline-pre-fase3.json` e qualquer regressão futura que compare saída exata em vez de agregados. <br><br>**Risco de produto — decidido, não é bug**: determinismo por save foi levantado como pergunta em aberto e RESPONDIDA pelo usuário: **é anti-requisito**, não requisito. Num jogo de carreira single-player, recarregar antes de uma final e ver o mesmo resultado transformaria qualquer derrota em algo contornável por save-scumming — a imprevisibilidade é o que dá peso às decisões. Decisão explícita: **nada de PRNG seedado por carreira**; os 90+ pontos com `Math.random()` cru estão corretos como estão; os 4 PRNGs locais existentes (chave, qualifying, geração de população) estão exatamente nos lugares certos — onde o MESMO torneio precisa de resultado estável — e não devem ser expandidos. O que o projeto precisa não é determinismo do jogo, é **reprodutibilidade sob instrumentação** (o harness/auditoria conseguir comparar antes/depois de forma confiável), que é outra coisa e tinha uma correção pequena. <br><br>**Correção aplicada**: `processAllClubsMonthly` (`clubs.js:221`) sequenciado — `Promise.all` trocado por laço `for...of` ordenado por `id` de clube (nunca a ordem de retorno de `.list()`, que não é contrato estável). Não muda comportamento de jogo (clubes continuam processados, só em ordem definida em vez de por corrida) — confirmado: nenhum clube lê estado de outro clube dentro de `processClubMonthlyUpdate`. **Validado**: o mesmo teste que antes divergia (harness com wrapper de contagem simples de `Math.random()` vs. wrapper com stack trace, mesma seed, 40+20 atletas, população chegando a 147) agora produz saída **byte-idêntica** nos dois casos — o heisenbug de reprodutibilidade-sob-instrumentação está fechado. <br><br>**Teste de paridade contra produção real** (metodologia Fase 0.1, mas contra `dayAdvanceCoordinator.js:advanceCareerDayOnce` — o ponto de entrada real do clique de "avançar dia", não a versão antiga deste harness): **quase idêntico, não 100%.** De 147 atletas, 146 batem exatos em todos os campos; 1 atleta diverge em `form`/`energy` (não em pontos/overall). Essa comparação foi ESTÁVEL entre os dois wrappers de instrumentação (não é mais um heisenbug) — é uma diferença determinística, pequena, ainda não isolada à linha. **Fase 4.2, item 1 — origem rastreada até o fim (não era o item aceito conscientemente).** Comparação de dump COMPLETO (todos os campos, não só os 5 originais) confirmou: exatamente 1 atleta diverge, "Rafael Alonso" — o MESMO prospect criado no boundary de 1º de fevereiro que motivou a investigação de `clubs.js` originalmente. `overall_rating`/`ranking_points`/`world_ranking_points` batem exatos (57 nos dois) — o resultado competitivo nunca diverge. O que diverge é um conjunto INTEIRO de campos de personalidade/comportamento (`ai_career_archetype`, `behavior_axes`, `preferred_side`, `pressure_profile`, `behavior_summary`, `partnership_patience`) mais `form`/`energy`/`fatigue`/`wealth`/`morale` como efeito colateral de uma trajetória de IA diferente a partir daí. Causa: `athletePersonalityLifecycle.js` deriva esses campos de um HASH SEEDADO PELO `id` DO PRÓPRIO ATLETA (`personalityAxes(athlete)`, `seeded(\`${athlete.id}:patience\`, ...)`, `hash(\`${key}:side\`)`) — determinístico, não usa `Math.random()`. Mas o `id` de um atleta recém-criado embute `Date.now()` (`makeId`), e `dayAdvanceCoordinator.js` envolve o avanço de dia real em `profileAction('advance-day', ...)`, cujo `recordAction` interno grava `at: Date.now()` — um tick a mais no relógio determinístico do processo, todo santo dia, que o harness não tinha antes. Ao longo de 31 dias esse desalinhamento acumula o bastante pra que um atleta criado no dia 31 nasça com um `id` (e portanto uma personalidade inteira hash-derivada) diferente da que nasceria em produção. **Corrigido parcialmente**: o harness passou a envolver seu próprio laço em `profileAction('advance-day', ...)`, igual produção — mas o resíduo (mesmo atleta, mesmos campos, valores diferentes de novo) **persistiu** depois dessa correção, confirmando que existe PELO MENOS MAIS UMA fonte de tick de relógio extra em algum lugar da cadeia de instrumentação/logging de produção que ainda não foi identificada. Não perseguido além disso — é cosmético por natureza (afeta só "sabor" de IA de atletas recém-criados no dia exato de um desalinhamento de relógio, nunca resultado competitivo), e cada tick adicional de rastreamento tem retorno decrescente. <br><br>**Recomendação sobre `docs/baseline-pre-fase3.json`**: **não recongelar por causa disto.** A divergência que motivou a preocupação (heisenbug de instrumentação, cascata populacional inteira) está resolvida — o que resta é um mecanismo diferente, plenamente rastreado até a origem (ainda que não 100% eliminado), comprovadamente cosmético e comprovadamente não-competitivo. Não justifica o custo de recongelar uma baseline que já serviu de referência pra várias fases. Fica registrado como resíduo conhecido e explicado, não como bloqueio. <br><br>**Casos de risco baixo, aceitos como estão**: os arrays fixos de 2-4 creates (`seasonLifecycle.js:187`, `partnerLifecycle.js:79`/`166`, `trainingLifecycle.js:80`/`87`) não foram sequenciados — não escalam com população, não tocam `Math.random()` direto, o único efeito possível é o sufixo do `id` trocar entre execuções (nunca os dados). Registrado como conhecido e aceito, não como pendência. | **Achado de arquitetura, RESOLVIDO nesta fase** — determinismo por save confirmado como anti-requisito de design (não bug); reprodutibilidade sob instrumentação corrigida (`clubs.js` sequenciado); paridade contra produção real quase completa (146/147, resíduo pequeno e estável, não bloqueia a baseline existente) |
| 28 | **Fase 4.2, item 2 — regime-check de 5 temporadas rodado pela primeira vez (pendente desde a Fase 2.7, adiado 3× por custo — a ~6min/temporada com a transação por dia, deixou de ser evento). Completou as 5 temporadas sem travar.** População oficial (900+100, seed `official-900-100-s1`), `DIAG_HEAP`/`DIAG_SIZES` ativos, limite de heap PADRÃO do Node (sem `--max-old-space-size`, de propósito — subir o limite mascararia exatamente o que a medição precisava confirmar). <br><br>**2A — OOM: REFUTADO por medição, não só por projeção.** Pico de `heapUsed` por temporada: 2026=673,3MB · 2027=766,1MB · 2028=831,9MB · **2029=830,2MB · 2030=831,5MB** — sobe nas primeiras 3 temporadas e **PLATÔ a partir da temporada 3**, não crescimento sem limite. `AthleteCareerLegacy` (a coleção sem poda por design que motivava a preocupação) foi de 10 linhas (mês 1) a 355 linhas (temporada 5) — cresce, mas devagar e sublinear, longe de explicar sozinho um heap que parou de subir. Total de bytes em todas as coleções também platôs (~5,4MB na temporada 5, viera de ~1,9MB na temporada 1) — as podas mensais já existentes (WorldEvent/CareerMessage/TeamRanking/Partnership, achados #20/#21) e a poda de `AnnualCareerReport` pro mais recente (achado #18, item de memória) estão dando conta. **Confirma a metade "corrigida" da projeção do achado #27** (menos churn de alocação = menos pressão de GC) **e não confirma a metade de risco** (o crescimento de `AthleteCareerLegacy` sem poda não estourou nada dentro de 5 temporadas, na escala atual de 900+100). Não generaliza automaticamente pra escalas maiores ou mais temporadas — é o que foi medido, não uma garantia permanente. <br><br>**2B — Rotatividade dos reais ausentes: CONFIRMADO ROTAÇÃO, NÃO EXCLUSÃO PERMANENTE.** Instrumentação existente desde a Fase 2.7 (`realAthletesNeverPlayedThisSeason`/`realAthletesNeverPlayedRotation`), executada pela primeira vez com `--seasons=5`. Contagem de reais que não jogaram por temporada: 12, 16, 20, 17, 19 (de 100) — oscila, não cresce monotonicamente. União across as 5 temporadas: 57 reais distintos ausentes em ALGUMA temporada. **Interseção (ausentes em TODAS as 5 temporadas): 0 reais — 0% da união.** Nenhum atleta real foi permanentemente excluído; cada ausência é intermitente, some numa temporada e volta na próxima. Confirmado também pelo resumo cumulativo: **0/100 reais nunca apareceram em nenhuma chave** ao longo das 5 temporadas — todos os 100 jogaram pelo menos uma vez. **Resposta direta**: é rotatividade, não exclusão — a Fase 5 não precisa endereçar isso como problema, só como comportamento esperado do sistema já registrado. <br><br>**2C — Composição de títulos e curva de dominância.** Títulos 100% reais por tier, agregado das 5 temporadas (de `season-tier-table.md`, que discrimina 6 dos 9 tiers — Bronze/Circuit Finals/Legacy Finals não itemizados nessa tabela, mas somados no total cumulativo de 400): Silver 78/80, Gold 29/40 (o tier mais disputado — é onde a maior parte dos títulos de bots aparece, 9/40 nas 5 temporadas), Platinum 25/30, Masters 50/50 (100% reais em TODAS as 5 temporadas, nenhum bot jamais venceu), Elite 47/50, Crown 20/20 (100% reais, todas as 5 temporadas). No total das 400 finais resolvidas (todos os 9 tiers): 372 100%-reais, 24 100%-bots, 4 mistas — reais dominam 93% dos títulos ao longo de 5 anos. **Top 20 (reais) por temporada**: 2026=20/20 · 2027=20/20 · 2028=19/20 · 2029=16/20 · **2030=15/20** — declínio GRADUAL (não em degrau/cliff), de 100% pra 75% ao longo de 5 anos. É consistente com o padrão esperado de uma narrativa de envelhecimento (não de diluição estatística abrupta): duplas de elite específicas (Coello&Tapia, Galán&Chingotto, Lebrón&Augsburger) mantiveram 100% de taxa de pareamento nas 5 temporadas inteiras, enquanto outras duplas históricas caem gradualmente a partir da temporada 2-3 (`season-tier-table.md`, seção "Duplas históricas") — o padrão "o topo persiste, o meio da tabela rotaciona" bate com aging/retirement, não com um efeito estatístico que afetaria a população toda por igual. **Não confirmado como causal** (não foi cruzado idade-por-idade dos atletas que saíram do Top 20 nesta rodada) — leitura do padrão agregado, não prova definitiva; suficiente pra não travar a Fase 4, mas uma verificação futura cruzando idade específica de quem sai do Top 20 fecharia a dúvida por completo, se algum dia importar mais que hoje. Baseline de referência pré-Fase-3 (19/20 na temporada 1) — a temporada 1 medida aqui (20/20) já supera essa referência, então o declínio de 5 temporadas parte de um ponto mais alto, não mais baixo. | **Achado de medição — as duas perguntas pendentes desde a Fase 2.7 respondidas por dados, não por projeção: OOM refutado (platô, não crescimento sem limite), rotatividade confirmada (0% de exclusão permanente)** |
| 29 | **Fase 4 — ranking rolling de 52 semanas implementado.** Substitui o acumulador vitalício (`world_ranking_points`/`rank_points` nunca decaíam desde a Fase 3 e antes — quem subia nunca descia, e os reais do seed ficariam eternamente no topo mesmo sem jogar) por uma janela móvel de 364 dias somando os **22 melhores resultados datados** — o mesmo modelo do circuito real. Race (ano civil) não muda; elegibilidade por tier continua lendo só o Circuito (confirmado, `EntryManager.js:resolveEntryRank` nunca leu `race_points`). <br><br>**Item 1 — verificação de intenção do corte de 20% de temporada, ANTES de remover (condição do pedido).** `finalizeSeason` (`seasonLifecycle.js`) e `applyTeamRankingSeasonCarryover` (`teamRanking.js`) cortavam 20% do Circuito do jogador uma vez por ano, desde o commit "v36" — sem mensagem descritiva, sem comentário de código, sem menção em nenhum doc de fase além de uma confirmação puramente funcional ("funciona e é idempotente", `docs/BETA_READINESS_PHASE10.md` §15) e um teste que só verificava a PRESENÇA do código (`test:ranking-carryover-v32`, checagem de string estática). **Nenhum registro de intenção de design encontrado — classificado como resíduo, removido, não substituído.** O jogo já tinha um mecanismo real de "temporada nova" (race_points, zerado no ano civil); o corte de 20% sobre o Circuito — que por definição nunca deveria resetar — era redundante com ele, nunca complementar. Efeito colateral corrigido junto: o bônus de prêmios de fim de temporada (`totals.rankPoints`, ex.: "dupla dominante") deixou de ser somado direto ao total e virou um resultado datado como qualquer outro — continua sendo concedido, só que sujeito à mesma janela/expiração. <br><br>**Arquitetura**: nova coleção `AthleteRankingResult` (`src/game-core/rankingWindow.js`) — um resultado datado por (atleta, torneio), nunca no documento clonado a cada escrita (mesmo precedente do `ranking_history` na Fase 4.0). Três pontos de escrita: `WorldTourLifecycle.js` (torneios de IA/bots, feedback imediato só nos atletas tocados), `tournamentLifecycle.js` (torneio do próprio jogador, mesmo tratamento — incluindo a correção do bug simétrico na dupla do jogador, que também incrementava pra sempre em vez de recalcular por média) e `circuitLifecycle.js:processWorldCircuit` (a ÚNICA passada semanal sobre a população inteira — único lugar que também PODA resultados expirados, decisão aprovada do item 2: apagar, não agregar, porque `career_wins`/`career_titles`/`career_titles_by_tier`/`recent_results` já preservam tudo que qualquer consumidor lê hoje, e agregar numa rotina de ~30 mil expirações/ano recriaria o próprio problema do achado #18). Duplas de IA já recalculavam `TeamRanking.ranking_points` do zero toda semana (média dos dois membros) — herdam o comportamento rolling automaticamente, sem mudança de código. <br><br>**Item 3 — migração por âncora sintética, aprovada como melhor que as duas opções originalmente propostas.** Uma linha sintética (`buildLegacySeedRow`, `finish: 'legacy_seed'`, `is_legacy_seed: true`, `tournament_id: null` — distinguível pra sempre, não só pelo enum) gravada no primeiro toque pós-migração, valendo o total vitalício cheio, datada nesse momento. Ocupa 1 dos 22 slots como qualquer resultado real — confirmado e documentado como intencional que um atleta ativo a descarte antes das 52 semanas se pontuar o bastante, e que ela expire e seja PODADA (não apenas ignorada) exatamente 364 dias depois se o atleta não jogar. **Zero reset visível no dia da migração** (confirmado por teste, ver abaixo). <br><br>**Verificação direta dos 4 efeitos de jogo (item 4C), `scripts/test-ranking-rolling-window-fase4.mjs`, 76/76 gates PASS — nenhum comportamento estranho, os dois gates críticos (4C.1/4C.3) não dispararam parada:** 4C.1 — um real do top 10 forçado a não jogar por 52 semanas mantém o total CHEIO até a semana 52 (sem decaimento antecipado) e cai a **zero** e sai do top 20 exatamente quando a âncora expira na semana 54 (>364 dias), enquanto atletas ativos de comparação convergem para o total esperado (~2200, 22×100) — confirma que o mecanismo é recomputação por janela, não decaimento incremental. 4C.3 — elegibilidade de tier móvel nos DOIS sentidos, testado com o mesmo laço: um atleta em Platinum (2000 pts) cruza pra Masters (2900 pts) imediatamente após pontuar; um atleta em Masters (2600 pts, salvo pré-Fase-4) cai pra Bronze quando sua âncora expira sem nenhum resultado novo — nenhum ranking fantasma em nenhuma direção. 4C.4 — dado de "defesa de pontos" confirmado existente e consultável (`AthleteRankingResult.filter({athlete_id})` mais `isResultExpired`, função pura) — UI não implementada, registrado como item pendente pra depois, conforme instruído. <br><br>**Custo do recálculo semanal (item 4B.4/Entrega item 3), medido, não presumido.** Temporada 1 isolada, mesma seed/escala oficial (`official-900-100-s1`, 900+100): **5min57,9s (Fase 4.1, sem rolling) → 6min27s (com rolling) — +8,1%**, tolerável. Regime-check completo de 5 temporadas: **48min55s no total** (vs. a extrapolação ingênua de "~30min" que a Fase 4.2 havia projetado a partir só da temporada 1) — o custo por temporada cresce mais que a curva 1,70× já registrada (achado #18) sozinha explicaria: temporada 1 ≈ 366,7s de laço diário, temporada 3 ≈ 701,1s (quase 2× a própria temporada 1 deste run), temporadas 4-5 recuam um pouco (625,0s/669,4s) mas não voltam ao patamar inicial. Atribuído à leitura de `AthleteRankingResult` inteira (uma vez por passada semanal, uma vez por resolução de torneio) escalar com o tamanho da própria coleção, que cresce ao longo de múltiplas temporadas mesmo com a janela de 52 semanas limitando o tamanho por atleta — um fator de custo novo que não existia antes desta fase. Não é motivo de alarme (heap seguiu longe do limite, ver 2A abaixo) mas é um custo real, não gratuito, e registrado como tal — sem generalizar para escalas maiores ou mais temporadas. <br><br>**Regime-check de 5 temporadas comparado com o de `5b32e23` (achado #28) — a curva de dominância caiu mais e por motivo mais claro, exatamente como o item 4C.2 previa.** Mesma seed/escala oficial. **Top 20 (reais) por temporada — ANTES (acumulador vitalício): 20/20 · 20/20 · 19/20 · 16/20 · 15/20 (100%→75%, declínio gradual). DEPOIS (rolling): 20/20 · 17/20 · 15/20 · 10/20 · 10/20 (100%→50%, declínio mais acentuado e mais cedo).** A tabela "Duplas históricas" (`season-tier-table.md`) confirma o mesmo padrão em outra métrica: as 3 duplas de elite mais fortes (Coello&Tapia, Galán&Chingotto, Lebrón&Augsburger) continuam 100% pareadas nas 5 temporadas inteiras — INALTERADO pela mudança de ranking, consistente com a leitura de que o topo persiste por mérito/narrativa, não por ranking congelado — enquanto pares do meio da tabela agora colapsam a 0% de pareamento MAIS CEDO que antes (vários já em 0% na temporada 2-3, alguns na própria temporada 2). **Heap por temporada: 762,3 · 832,7 · 888,4 · 894,2 · 932,5MB** — mais alto que o achado #28 (673,3...831,5MB) em toda temporada, e SEM platô claro até a temporada 5 (ainda subindo, embora desacelerando) — consistente com o custo adicional da nova coleção, mas nenhum OOM ocorreu e a folga até o limite padrão do Node continua grande. **Composição de títulos cumulativa (400 finais): 381 100%-reais, 17 100%-bots, 2 mistas** (era 372/24/4) — reais venceram MAIS títulos, não menos, apesar do ranking cair mais rápido: confirma que RANKING (atividade recente, sujeita a decaimento) e FORÇA COMPETITIVA (`overall_rating`, usado pelo motor de partida, nunca por pontos de ranking) são coisas diferentes — o mecanismo novo muda quem aparece no topo da TABELA sem mudar quem VENCE as partidas. <br><br>**Rotatividade dos reais ausentes (2B) — mudou de 0% pra 3,9% de interseção, registrado para a atenção da Fase 5, não escondido.** Por temporada: 9, 14, 24, 23, 21 (união de 51 distintos). **Interseção (ausentes em TODAS as 5 temporadas): 2 reais — Pablo García (OVR 82) e Eduardo Agustín Torre (OVR 79)** — não são os reais de OVR mais baixo da base (checado no registro), então não é simplesmente "pior do elenco = excluído"; a causa mais provável é um efeito dinâmico da seleção de torneio pela IA (`chooseTournament`/`pairScore`) interagindo com um ranking agora sujeito a queda, não investigada a fundo aqui (fora do escopo desta entrega, que era medir e comparar, não depurar cada exclusão individual). **Isso é uma mudança real em relação ao achado #28 (que media 0% de exclusão permanente)** — pequena (2/100), mas real, e é exatamente o tipo de dado que o próprio enunciado da Fase 4 já antecipava que a Fase 5 (agenda dos reais) precisaria herdar. Não travou esta fase (não é um dos dois gates críticos 4C.1/4C.3), mas fica registrado explicitamente, não como rodapé. <br><br>**Gap conhecido e aceito, não corrigido nesta fase**: o `TeamRanking` da dupla do PRÓPRIO jogador só é recalculado quando ele termina um torneio (`tournamentLifecycle.js`) — ao contrário de duplas de IA (recalculadas toda semana por `updateTeamRankings`), não decai passivamente se o jogador parar de jogar. Afeta só essa única linha (aba Duplas), não o "topo vitalício" que esta fase ataca. <br><br>**Correção de registro (Fase 4.3, achado #30)**: a atribuição de custo acima ("escalar com o tamanho da própria coleção, que cresce ao longo de múltiplas temporadas") estava ERRADA — medição direta na Fase 4.3 mostrou que `AthleteRankingResult` ESTABILIZA (não cresce sem limite) a partir da temporada 2. Ver achado #30 para o número certo e a causa real do crescimento de custo. | **Feature implementada e medida — mecânica verificada por teste direto (76/76, os dois gates críticos passaram sem comportamento estranho), custo real mas tolerável, curva de dominância cai mais e por motivo mais claro como esperado; um dado novo (2% de exclusão permanente, era 0%) registrado para a Fase 5; atribuição de causa do custo corrigida pelo achado #30** |
| 30 | **Fase 4.3 — a exclusão permanente de 2 reais (achado #29) é armadilha estrutural, não das duas hipóteses levantadas — uma terceira, mais precisa, confirmada por código E por rastreamento ao vivo de 5 temporadas.** Instrumentação temporária (`DIAG_TRACE_ATHLETES`, revertida após medir) acompanhou Pablo García e Eduardo Agustín Torre semana a semana: posição, pontos, tier, status de parceria e a decisão de `chooseTournament` a cada semana, nas 5 temporadas inteiras. <br><br>**Hipótese 1 (retroalimentação: ranking baixo → inelegível pros tiers que a IA prioriza → joga ainda menos) — REFUTADA.** `evaluateTournamentEntry` sempre reconhece Bronze/Silver (`min_ranking:0`) como elegíveis quando oferecidos naquela semana — confirmado em toda ocorrência do log (`elegivel-em=Bronze`/`Silver` sempre que esses tiers apareciam em `ofertas-semana`). A elegibilidade nunca falhou pelo motivo hipotetizado. <br><br>**Hipótese 2 (sem parceiro, causa simples) — PARCIALMENTE CONFIRMADA, mas não é a explicação completa.** Os dois começam a carreira com `partner_id: null` no registro-fonte (`realAthletesRegistry.json`) e passam a temporada 1 inteira (52+ semanas) sem parceiro — confirmado, `partner=NENHUM` em toda semana de 2026. Isso sozinho já explica a temporada 1. Mas os dois EVENTUALMENTE são pareados pelo mercado de IA (Eduardo em 2027-05, com o bot "Emiliano Alonso"; Pablo em 2028-03, com o bot "Lucas Giménez 943") — e a exclusão CONTINUA depois disso, então "sem parceiro" não é a causa nas temporadas 2-5. <br><br>**Mecanismo real, confirmado por código e por dado ao vivo: capacidade fixa nos tiers de acesso livre, sem fila nem novo aviso quando o excesso é descartado.** `WorldTourLifecycle.js` monta o campo de cada torneio assim: `entrants = assignments.get(tournament.id)` (pares que ESCOLHERAM aquele torneio) → se `entrants.length < drawSize`, o backstop do achado #22 completa a chave (mecanismo já corrigido e funcionando); mas a linha seguinte, incondicional, é `ordered = entrants.sort(pairScore desc).slice(0, drawSize)` — se MAIS pares escolheram aquele torneio do que há vagas (oversubscrição, o caso oposto ao que o achado #22 corrigiu), o excesso é descartado SILENCIOSAMENTE, sem aviso (o único `console.warn` existente cobre o caso de POUCOS pares, nunca o de pares demais) e sem qualquer fila/nova tentativa na semana seguinte. Bronze e Silver (`mainDrawSize:16` cada, ~24+16=40 eventos/temporada) têm capacidade FIXA de ~640 vagas/temporada — e são justamente os únicos tiers que um atleta com ranking baixo pode escolher, então TODO atleta nessa situação (não só os dois reais) converge pra essas ~640 vagas. **Confirmado numericamente**: nas 5 temporadas rastreadas, Eduardo e Pablo tiveram **92 decisões de "jogar" registradas (`decisao=play`) — e 0 delas (0/92) resultou em um único ponto ganho.** Toda vez que a IA corretamente escolheu Bronze ou Silver, o par perdeu a corte por `pairScore` (que pesa `overall_rating` médio da dupla — um parceiro bot fraco derruba a média o bastante pra perder a disputa por vaga, mês após mês). O "ponto sem retorno" de Eduardo é a semana 54 (2027-01-03: a âncora sintética de migração expira sem nenhum resultado novo — pontos caem de 1591 para 0 e nunca mais saem de lá, nem depois de arranjar parceiro e tentar repetidamente); o de Pablo é análogo. <br><br>**Isso é a MESMA classe de bug citada no pedido (`processWorldCircuit` da Fase 1.5: truncamento por população/capacidade disfarçado de corte por mérito) — confirmado, não só por analogia.** A elegibilidade (a regra) está certa; a CAPACIDADE (a prateleira) não escala com quem precisa dela, e ninguém que perde a vaga é reconsiderado depois. <br><br>**Piso proposto — não implementado, registrado para o desenho da Fase 5**: a versão ingênua de "piso" (só garantir elegibilidade em `minRanking:0`) NÃO resolveria nada, porque a elegibilidade já está correta — o problema é a fila. Um piso que funcione precisa de uma das duas: (a) reservar um número de vagas por chave de Bronze/Silver para pares sem NENHUMA outra opção elegível naquela semana (prioridade, não `pairScore` puro, para essa fração da chave), ou (b) escalar a oferta de Bronze/Silver (mais eventos/temporada, ou `mainDrawSize` maior) com o tamanho da fatia da população que só tem esses dois tiers como opção — que cresce à medida que o ranking rolling (Fase 4) empurra mais atletas pra baixo com o tempo. Sem uma das duas, qualquer redesenho da Fase 5 que só amplie elegibilidade herda a mesma armadilha. <br><br>**Decisão de crescimento de `AthleteRankingResult` (item 2)**: contagem de linhas por fim de temporada, mesma rodada de rastreamento — temporada 1: **4528** (inflada por um efeito de migração de uma vez só — toda a população ganha uma âncora sintética no primeiro toque); temporada 2: **3891** (queda de -14% assim que as âncoras da temporada 1 começam a expirar, na semana 54 — a poda funcionando exatamente como desenhada); temporada 3: **4165**; temporada 4: **4357**; temporada 5: **4555**. **ESTABILIZA — não cresce sem limite.** O crescimento residual entre temporadas 2-5 (~+5-7%/temporada) acompanha o crescimento lento da própria população (novos prospects gerados mensalmente), não um vazamento da coleção. **Decisão: nada a fazer — a poda por idade (364 dias) já é suficiente, não é o mesmo padrão das 6 ocorrências anteriores desta auditoria** (essas eram coleções SEM NENHUMA poda; esta tem poda e ela funciona, visível no próprio corte -14% entre temporada 1 e 2). <br><br>**Correção do registro do achado #29**: a atribuição anterior do crescimento de custo por temporada ("`AthleteRankingResult` escala com o tamanho da própria coleção, que cresce ao longo de múltiplas temporadas") estava ERRADA — a coleção NÃO cresce como se pensava depois da temporada 1. O crescimento de custo real (temporada 1 ≈366,7s de laço diário → temporada 3 ≈701,1s) é melhor explicado pelo padrão JÁ REGISTRADO (achados #18/#23: o save inteiro cresce a cada temporada — `Tournament`/`Partnership`/`AthleteProfile` sobem de tamanho e de contagem todo ano) somado a uma leitura de `AthleteRankingResult` que é cara mas **aproximadamente CONSTANTE** (~4000-4600 linhas/leitura, não crescente) — um imposto fixo a mais por semana/torneio, não um imposto que composto. <br><br>**Consulta indexada por atleta (item 2.4) — não é possível nesta arquitetura, e não ajudaria mesmo se fosse.** `CareerEntityRepository` (a camada de armazenamento local inteira) não tem NENHUM mecanismo de índice — `.filter()`/`.list()` fazem uma varredura linear completa do array em memória, sempre, para qualquer consulta. Uma consulta "por atleta e janela de data" custaria exatamente o mesmo que a leitura atual (`O(tamanho da coleção)`) SE feita uma vez só; feita UMA VEZ POR ATLETA (a alternativa cogitada) custaria `O(população × tamanho da coleção)` — estritamente PIOR, não melhor. O padrão atual (uma leitura da coleção inteira por passada semanal, agrupamento em memória via `Map`) já é o ótimo possível dado que este armazenamento não tem índice — não há consulta mais barata disponível nesta arquitetura. <br><br>**Implicação para a Fase 5 (item 3)**: reais venceram 381/400 títulos (95,25%) nas 5 temporadas MESMO com o ranking agora sujeito a queda — confirma que força competitiva (`overall_rating`, usada pelo motor de partida) e posição de ranking (atividade recente) são coisas diferentes, e que a Fase 4 não criou um problema de "reais não vencem mais". **O problema que resta não é fazer os reais vencerem — já vencem sem agenda dedicada.** É fazê-los APARECER nos lugares certos: presentes em Crown/Elite/Masters (onde o corte de ranking naturalmente os favorece, dado seu histórico) e, no MÍNIMO, presentes de forma proporcional em Bronze/Silver quando caem — o que hoje não acontece por causa exata do mecanismo de capacidade fixa acima. **Isso reduz o escopo da Fase 5** de "balancear dominância" (problema mal definido, e a dominância já existe e é saudável) para "distribuir presença" (problema mais estreito: garantir que ranking baixo não vire clausura, resolvendo o gargalo de capacidade do item 1 antes de qualquer agenda nova). | **Diagnóstico confirmado por código e por dado ao vivo — armadilha estrutural real (capacidade fixa nos tiers livres, sem fila), não implementada por instrução explícita; crescimento de `AthleteRankingResult` estabiliza, nada a fazer; consulta indexada por atleta seria pior, não melhor, nesta arquitetura; escopo da Fase 5 reduzido de "balancear dominância" para "distribuir presença"** |
| 31 | **Fase 4.4 — o gargalo do achado #30 não é caso de borda: mediu-se em toda a população, em duas escalas, e a resposta muda o desenho da Fase 5.** Instrumentação temporária (`DIAG_CAPACITY`/`DIAG_INJECT_CLIMBER`, revertida após medir) rodada no regime-check completo de 5 temporadas, duas vezes — escala oficial (900 procedurais + 100 reais = 1000) e escala reduzida (400 procedurais + 100 reais = 500), mesma seed `official-900-100-s1`. <br><br>**Item 1 — demanda vs. capacidade em Bronze+Silver, população inteira (1000).** Razão demanda/capacidade (escolhas de dupla por semana, somadas na temporada, contra as ~640 vagas fixas): **9,99× (temporada 1) → 15,29× → 15,35× → 15,56× → 15,11×** (a temporada 1 é mais baixa porque a população/duplas ainda estão se formando). **Duplas que escolheram jogar em Bronze/Silver mas nunca entraram em NENHUMA chave naquela temporada: 228 de 402 (56,7%) → 349/517 (67,5%) → 334/511 (65,4%) → 311/510 (61,0%) → 340/499 (68,1%)** — em regime estável, **entre 6 e 7 de cada 10 duplas que tentam a base ficam de fora inteiramente**, não é caso de borda, é a norma. Confirma a leitura do próprio pedido: a proporção do circuito real é o oposto da nossa (FIP Tour: 200+ eventos de base contra 26 no topo — a base absorve todo mundo; aqui, Bronze+Silver somam ~40 eventos/temporada contra dezenas de tiers mais altos combinados) — **nenhuma regra de reserva de vaga resolve uma razão de 15×**, é subdimensionamento de calendário, exatamente a distinção que o item 1.2 pedia para checar. <br><br>**Item 2 — comparação 500 vs. 1000, nos 5 eixos pedidos (regime-check completo de 5 temporadas em cada escala):**<br>**(a) Capacidade** — cortar a população pela metade CORTA a razão demanda/capacidade quase pela metade (4,48×→6,93×→7,14×→7,13×→6,66×) e o descarte-apesar-de-escolher quase pela metade (54/208=26,0% → 116/275=42,2% → 112/269=41,6% → 113/270=41,9% → 90/251=35,9%) — **melhora real, mas não resolve**: mesmo a 500, mais de 1 em cada 3 duplas que tentam Bronze/Silver em regime estável ficam de fora. Nenhuma chave incompleta em nenhum dos dois cenários (0/80 toda temporada, os dois lados) — a montagem de campo (achado #22) nunca é o problema; a capacidade É o problema. <br>**(b) Presença dos reais** — título 100%-real quase idêntico nas duas escalas (1000: 373/400=93,25%; 500: 378/400=94,5%) — confirma achado #30, escala não muda QUEM VENCE. Crown e Elite IDÊNTICOS nas duas escalas (Crown 4/4 real toda temporada; Elite 9-10/10 real toda temporada, nos dois cenários) — os cortes de acesso do topo já são exclusivos o bastante pra não sentir a diferença de população. A curva de dominância do Top 20 declina MENOS a 500 (100%→50%: 20,15,12,12,10) do que a 1000 (100%→35%: 20,15,11,8,7) — menos concorrência total dilui menos o topo. Reais nunca-jogaram (toda a temporada, toda a rodada): 1/100 nos dois cenários (Pablo García a 1000; Luciano Capra, OUTRO atleta, a 500 — confirma que é probabilístico/sensível a instrumentação, achado #27, não um alvo fixo). <br>**(c) Repetição de adversários** — proxy por co-entrantes de chave (não é rodada-a-rodada, o motor não simula isso). Taxa de reencontro (visto em ≥2 anos distintos): reais 44,9% a 1000 → 49,1% a 500; bots 43,3% → 52,2%. Efeito real mas modesto (+4 a +9 pontos percentuais, não dobra) — e o dado mais importante é que **JÁ a 1000, quase metade dos adversários de um real recorrem em anos diferentes — contradiz a expectativa de "com 970 bots anônimos, provavelmente não são" possíveis rivalidades.** Corrigido: rivalidades já são estatisticamente plausíveis na escala atual. <br>**(d) Velocidade de ascensão** — atleta sintético (overall 85, parceiro garantido e protegido desde o dia 1, isolando deliberadamente esta pergunta da armadilha do achado #30), começando no fundo do ranking. Top 100: **20 meses (1000) → 18 meses (500)**, só 2 meses de diferença (~10%). Top 20: **30 meses → 22 meses** (8 meses, ~27% mais rápido). Top 10: **47 meses → 35 meses** (12 meses, ~26% mais rápido, quase uma temporada inteira). Posição final após 5 anos: #7 (1000) vs #5 (500). **O efeito de cortar a população pela metade NÃO é "duas vezes mais rápido" em nenhum ponto da escada — é modesto para sair da obscuridade (top 100) e mais relevante pra entrar na elite (top 10-20).** <br>**(e) Custo** — 1000: 47min8s (2828s), heap 783,3→958,1MB. 500: 42min56s (2576s), heap 695,4→890,9MB. **Cortar a população pela metade economiza só 8,9% do tempo de parede** — DESPROPORCIONAL: o custo do regime-check não é dominado pelo tamanho da população (a contagem de torneios/temporada é FIXA por config, `circuitCatalog.js`, independente de `--proceduralAthletes` — só o tamanho do array de duplas iterado por torneio encolhe). **Corte de população não é argumento de custo por si só.** <br><br>**Item 3 — inventário de controles de progressão, sem implementar nenhum.** <br>**(1) Qualidade do parceiro como porta de acesso — ACHADO PRINCIPAL: o mecanismo já existe, está bem desenhado, e está COMPLETAMENTE DESCONECTADO.** `calculatePartnershipInterest` (`src/players/teamCompatibility.js:67`) calcula um `score` de interesse real e bem modelado — compatibilidade tática (35%), REPUTAÇÃO do jogador (30%), progresso de RANKING do jogador (`rankingProgress`, 35%) MENOS demanda do candidato ser elite (`eliteDemand`, -15%, um atleta muito bem-rankeado é mais difícil de atrair) — com `level`/`reasons`/`requirements` textuais corretos ("sua reputação ainda limita o interesse", "melhore ranking, reputação ou condições da proposta"). **Mas o campo `available` — o único que `buildInitialPartnerOffers` (`src/lib/partnerOfferRules.js:7`) realmente usa pra decidir se o candidato aparece como opção — NUNCA lê o `score`: `available: athlete?.career_status !== 'aposentado'`.** Um jogador no fundo do ranking, com reputação zero, pode ver e contratar IMEDIATAMENTE o melhor atleta do jogo — `startPartnership` (`src/lib/partnershipSystem.js:254`) também não tem nenhuma checagem própria. Toda a máquina de "convencer alguém melhor" já está escrita e correta — só nunca foi ligada ao portão. **Este é o controle mais barato e de maior alavancagem disponível para a Fase 5**: religar `available` (ou uma checagem equivalente em `startPartnership`) ao `score` já calculado transforma a subida em algo narrativo (precisa construir reputação/ranking pra atrair parceiros melhores) sem nenhuma nova mecânica de jogo, só destravar a existente. Achado adicional: `generatePartnerProposals` (`partnershipSystem.js:593`) é código morto (zero chamadores) — um sistema de propostas paralelo e mais simples que nunca foi ligado a nada; não confundir com o fluxo real (`PartnerHub.jsx`→`partnerOffers.js`→`partnerOfferRules.js`). <br>**(2) Pontos por tier e cortes de acesso** — já em config (`circuitCatalog.js:TOURNAMENT_TIER_CONFIG`, Fase 3A), `minRanking`/`rankPoints`/`mainDrawSize` por tier, central e já tunável sem nova construção. <br>**(3) Ritmo de evolução de atributos** — sistema maduro e já calibrado (`trainingSystemV2.js`): `getAttributeDevelopmentCeiling` (teto por atributo, dependente de `potential`, com bônus de especialização por estilo — Fase 13.1 já ajustou o padrão pra deixar uma carreira "média" abaixo do Top 10-20 real de propósito), `getDevelopmentWindow` (retorno decrescente conforme se aproxima do teto — multiplicador de 1× a 0,08×), `getDiminishingMultiplier` (fadiga de repetição na mesma semana). Não precisa de nada novo — é lever já existente. <br>**(4) Janela dos 22 melhores/52 semanas** — já construída na Fase 4 (`rankingWindow.js`) — já pune resultado isolado sem consistência sustentada, por design. <br>**Conclusão do item 3: dos 4 controles pedidos, 3 já existem prontos e tunáveis (pontos por tier, ritmo de atributos, janela de consistência); o quarto (parceiro como porta de acesso) existe CALCULADO mas não aplicado — é a peça que falta, e é a mais barata de destravar.** | **Medição completa nas duas frentes pedidas — capacidade confirmada como subdimensionamento estrutural (15× a 1000, 7× a 500, nenhuma das duas "perto de 1"); comparação 500×1000 nos 5 eixos entregue (capacidade melhora ~50%, presença no topo idêntica, reencontro de adversários já alto nas duas escalas, ascensão 10-27% mais rápida a 500 dependendo do estágio, custo cai só 9% — corte populacional não se paga em custo sozinho); inventário de progressão entregue — o controle do parceiro já existe calculado e só precisa ser ligado ao portão de aceitação, não construído do zero** |
| 32 | **Fase 5 — a curva de capacidade em 4 tamanhos de calendário não estabiliza dentro da faixa segura, e o 4º cenário revelou um teto de código pré-existente, não um dado de capacidade.** Instrumentação temporária (`DIAG_CAPACITY`, generalizada por tier — não só Bronze/Silver desta vez, revertida após medir, zero ocorrências restantes por grep), harness de 2 temporadas, população oficial (900+100, seed `official-900-100-s1`), quatro valores de `TIER_EVENTS_PER_YEAR` para Bronze+Silver (os únicos tiers `minRanking:0`, conforme instruído — Gold pra cima intocado nos 4 cenários): 40 (atual, 24+16 — reaproveita os números já publicados no achado #31), 80 (48+32), 150 (90+60), 250 (150+100). <br><br>**Item 1 — a razão agregada cai, a exclusão real não se move.** Razão demanda/capacidade em regime (temporada 2): **15,29× (40) → 10,97× (80) → 5,93× (150)** — cai quase proporcionalmente ao aumento de vagas, sem sinal de cotovelo entre os três. Mas **duplas distintas que escolheram Bronze/Silver e nunca entraram em nenhuma chave na temporada: 349/517=67,5% (40) → 339/506=67,0% (80) → 346/511=67,7% (150)** — parado, essencialmente o mesmo número, apesar de mais que dobrar a capacidade (640→2400 vagas/temporada). **Leitura**: o gargalo não é (só) volume de vagas — é o mecanismo de seleção. `WorldTourLifecycle.js` ordena o pool inteiro por `pairScore` (peso dominante: `overall_rating` médio da dupla) e trunca em `drawSize`, toda semana, sem rotação nem memória de quem ficou de fora — cada vaga nova tende a ser preenchida pela MESMA fatia de duplas de maior `overall_rating` que já preenchia as vagas antigas. Conecta direto com o piso proposto no achado #30 (reservar fração das vagas por PRIORIDADE, não `pairScore` puro, pra quem não tem outra opção elegível na semana) — nas três escalas medidas, o ganho marginal da expansão de calendário sozinha sobre a exclusão real é zero; a razão agregada melhora, quem fica de fora não muda. Chaves incompletas: 0 em todos os cenários que resolveram (0/80, 0/239, 0/379) — o preenchimento de reserva (achado #22) nunca é o fator limitante em nenhum tamanho testado. Distribuição de títulos por classificação (2 temporadas, todos os tiers): 80 → 227/239 100%-reais (95,0%); 150 → 365/379 100%-reais (96,3%) — reais continuam vencendo 95-96% dos títulos em qualquer tamanho de calendário, inclusive Bronze/Silver isoladamente (95/96 a 80; 179/180 a 150), confirmando de novo a leitura do achado #30 (força competitiva e presença/capacidade são eixos diferentes). Custo: 80→150 (quase dobrar Bronze+Silver) custou só +4,7% de tempo de parede em 2 temporadas (17min44s → 18min34s) — mesmo padrão do achado #31 (custo dominado pelo laço diário sobre a população inteira, não pela contagem de torneios/semana). <br><br>**Item 1, cenário 250 — não é um 4º ponto de capacidade, é a descoberta de um teto de código.** A 150 de base (188 torneios/temporada) o circuito resolve normal (190+189=379 torneios, 0 incompletas). A 250 de base (288 torneios/temporada), MESMA população: **0 torneios resolvidos em QUALQUER tier, nas duas temporadas, sem nenhum erro/aviso no log.** Causa raiz, confirmada por leitura de código (pré-existente, não introduzida nesta fase): `resolveCompletedWorldTourEvents` (`WorldTourLifecycle.js:156`) busca torneios pendentes com `entities.Tournament.list('-start_date', 300)` — **limite fixo de 300 linhas, ordenado pelas datas mais recentes primeiro**; `ensureFutureTournamentsInternal` (`career.js:419`) cria calendário com **15 meses de antecedência** a cada virada de mês. Com 288 torneios/temporada, 15 meses de horizonte mantêm ~360 torneios simultaneamente existentes — os FUTUROS (ainda não pendentes) têm `start_date` mais recente que qualquer pendente, ocupam sozinhos as 300 posições da consulta e empurram pra fora da janela os que precisam ser resolvidos, que nunca entram em `pending` — a função retorna `{resolved:0}` silenciosamente todo santo dia (`WorldTourLifecycle.js:165`, retorno antecipado antes de montar campo, por isso nenhum aviso de chave incompleta). Explica também um número que parecia contradizer o "0 resolvidos": o resumo cumulativo relatou bots com "média 17,39/mediana 18" torneios jogados — não vem da simulação, é o valor ESTÁTICO de história de fundo atribuído na criação do elenco (`rankingPopulation.js:175`: `tournaments_played: Math.max(1, 6 + (seed % 28))`, faixa 6-33, média ≈19,5 — mesma ordem de grandeza), nunca incrementado porque a resolução real nunca rodou uma vez. Pela conta do teto (300 ÷ 1,25 de horizonte), o limite atual fica em torno de **240 torneios/temporada no total, ~200 eventos de base/ano** dado o total fixo de 38 dos tiers mais altos — 150 de base (188/ano) fica abaixo e funciona limpo; 250 de base (288/ano) fica muito acima e quebra por completo. **Não corrigido nesta entrega** (fora do "nada implementado até aprovar os dois" combinado para este ciclo) — mas é informação necessária pra decidir qualquer calendário perto ou acima dessa linha: já não é um limite teórico, é uma parede já tocada. <br><br>**Resposta ao pedido ("onde a curva estabiliza")**: não estabiliza, nas escalas onde foi possível medir — a razão agregada cai proporcionalmente sem cotovelo visível entre 40/80/150, a exclusão real fica travada em ~67% nas três, e ampliar mais esbarra no teto de código antes de haver qualquer chance de ver se a fração de exclusão cede em escala maior. Nenhum número escolhido, conforme instruído. <br><br>**Item 2 — desenho do portão de parceria proposto, não implementado.** `calculatePartnershipInterest` (`teamCompatibility.js:67`, já descrito no achado #31) segue correto e desconectado — `buildInitialPartnerOffers` (`partnerOfferRules.js:14`) só lê `career_status`, nunca o `score`. Achados novos desta fase: (a) existe um SEGUNDO gate pré-existente e já ativo, independente — `getAvailablePartners`/`getLockedPartners` (`career.js:309`/`320`) filtram por NÍVEL DE XP do jogador (`levelForXp`, `padel.js:162`, limiares 0/500/3000/10000/25000/50000), eixo diferente de reputação/ranking; qualquer gate novo precisa conviver com este, não substituí-lo às cegas; (b) o catálogo de candidatos do PartnerHub (`buildAthleteCatalog`, `athleteCatalog.js:11`) é ESTÁTICO — 100 reais + 240 fictícios (`athleteGenerator.js`, `ranking_position`/`world_rank` fixos em `25+índice`, nunca atualizados pela simulação) — totalmente separado do mundo dinâmico de ~1000 atletas medido nos achados #28-31; o `score` calculado ainda é válido (usa a posição do JOGADOR, que é dinâmica), mas o ranking do candidato nunca muda ao longo da carreira; (c) três caminhos de contratação, hoje inconsistentes: onboarding (`PartnerSelection.jsx`→`getSuggestedPartnerTerms`, `partnerLifecycle.js:45`/`57`) JÁ tem fricção por gap de nível (duração 30/45/60 dias, `prizeSplit` 45-65% conforme `overall(bot)-overall(jogador)`); convite direto no PartnerHub (`handleInvite`→`startPartnership(profile,bot,60,50)`) usa termos FIXOS, ignora o gap; aceitar oferta pré-formatada (`acceptPartnerOffer`→`partnerOfferRules.js:14`) também usa termos FIXOS por posição no array (`[60,45,90,60][index]`, `prizeSplit:50`) — ligar um gate por `score` sem unificar os três deixa dois dos três caminhos furando a fricção que o pedido descreve. <br><br>**Desenho proposto — 3 faixas por `score`, sem corte rígido**: `score < T_low` → candidato invisível na lista, com `reasons`/`requirements` (já existentes no retorno da função) explicando o eixo que falta; `T_low ≤ score < T_high` → aparece com termos piores, reusando `getSuggestedPartnerTerms` (gap-based) em vez dos termos fixos dos dois caminhos do PartnerHub, opcionalmente condicionado a um resultado recente mínimo; `score ≥ T_high` → termos normais (comportamento de hoje). Cobre as três alternativas do pedido (oferta recusada/invisível, contrato mais caro, exigência de resultado) sem corte binário único. **Calibração de partida** (cálculo manual contra a fórmula real, valores de perfil inicial reais — não simulado, a verificar após implementar): jogador novo (`careerRank≈1000`, `reputation≈10`) vs. candidato equivalente ≈36, vs. top 20-50 ≈23-24; jogador de meio de carreira (`careerRank≈200`, `reputation≈50`) vs. equivalente ≈66, vs. top 20-50 ≈53-55. Com `T_low≈30`/`T_high≈55`: novo consegue equivalente (36, faixa fricção), não consegue top 20-50 (23-24, abaixo de 30, invisível); meio-de-carreira consegue equivalente (66, aberto) e fica na fronteira fricção/aberto pro top 20-50 (53-55, perto de 55) — bate com o alvo declarado (#1000 não chega ao top 50 no dia 1; top 20 é meta de meio de carreira, não escolha de menu). Pergunta em aberto, não resolvida: não confirmado se ofertas expiradas (`expires_career_date`, 10 dias) são limpas de forma a `ensureInitialPartnerOffers` (`partnerOffers.js:56`, só regenera quando `existing.length===0`) voltar a disparar — dependência direta da narrativa de "desbloqueio de meio de carreira", a confirmar na implementação. Medir o efeito no ritmo de ascensão (item 2.4 do pedido, "o controle que substitui a ideia de cortar população") fica explicitamente para depois da aprovação e implementação — não medido nesta entrega. Ver [FASE-5-RELATORIO.md](FASE-5-RELATORIO.md) para a curva completa e o desenho detalhado. | **Curva de capacidade entregue nos 3 cenários que funcionaram (15,3×→11,0×→5,9× de razão, ~67% de exclusão real travada nos três — sem cotovelo, capacidade sozinha não resolve a exclusão); 4º cenário (250) não é dado de capacidade — é a descoberta de um teto de código pré-existente (~200 eventos base/ano, consulta de 300 linhas × horizonte de 15 meses), registrado, não corrigido; desenho do portão de parceria entregue como proposta de 3 faixas por `score` (sem corte rígido), calibração de partida verificada por cálculo contra a fórmula real; nada implementado — ambos aguardam aprovação** |
| 33 | **Fase 5.1 — um dado suspeito confirmado como contaminação real, agenda de tier e portão de parceria implementados (aprovados na Fase 5), e a agenda de tier sozinha resolveu o que 4 cenários de calendário não resolveram.** <br><br>**Item 0 — verificação de `tournaments_played` (pedida antes de qualquer mudança de comportamento).** Confirmado: `rankingPopulation.js:175` escrevia um valor ESTÁTICO de história de fundo (`Math.max(1, 6 + seed%28)`, faixa 6-33, média≈19,5) no MESMO campo que `WorldTourLifecycle.js:383` incrementa a cada torneio realmente resolvido — um único contador, duas origens (seed pra bots, só jogo pra reais, que nunca recebem o seed — confirmado, nenhum bootstrap de real grava o campo). Levantados TODOS os usos de `tournaments_played`/"torneios disputados" na auditoria: os três números da Fase 2 (Resultado A/B/C, pré-numeração de achados) usavam o campo cru e estavam contaminados; achados #31/#32 usavam `DIAG_CAPACITY`/`tournament-results.csv` (não tocam o campo) e NÃO precisam de correção. **Nenhuma conclusão qualitativa se inverte — a contaminação, corrigida, torna o domínio real MAIS forte, não mais fraco**: Resultado B (970 bots, 1 temp.) "reais 0,58 · bots 20,15" — descontando o seed, bots real ≈0,6, PRATICAMENTE IGUAL aos reais (a comparação original era seed-contra-jogo, não bot-contra-real); Resultado C (970 bots, 5 temp.) "reais 31,5 · bots 23,1" (lido como "mesma ordem de grandeza") — descontando o seed, bots real ≈3,6, reais jogam ~8,7× mais que bots, não ~1,4×. **Número novo, campo já corrigido, ANTES de qualquer mudança de comportamento** (baseline isolado desta fase, 900+100, 2 temporadas): `reais: média 40,83/mediana 30 · bots: média 2,81/mediana 0` — **a mediana de bots é zero**: mais da metade da população procedural nunca joga um torneio em 2 temporadas inteiras, achado que a contaminação escondia por completo atrás de um piso artificial de 6-33. **Corrigido**: campo renomeado pra `backstory_tournaments_played` (`rankingPopulation.js`) — mesmo valor/fórmula, não compartilha mais o contador; confirmado por grep, `tournaments_played` nunca foi lido em nenhuma tela (`src/**/*.jsx`), renomeação sem efeito visual. <br><br>**Item 1 — agenda de tier implementada (`TIER_EVENTS_PER_YEAR` intocado, como instruído).** Três mudanças, todas restritas a Bronze/Silver (`minRanking:0`): **(a) teto** (`EntryManager.js`) — uma dupla já classificada pro Gold (rank ≤ `TOURNAMENT_TIER_CONFIG.Gold.minRanking`, hoje 800, referenciado do próprio config) agora é `INELIGIBLE` pra Bronze/Silver — antes só havia piso, nunca teto, e um rank #1 continuava "elegível" pra Bronze. Efeito colateral notado: `evaluateTournamentEntry` é compartilhada com a inscrição do PRÓPRIO jogador — um jogador Gold-caliber também para de poder se inscrever em Bronze/Silver, considerado correto por desenho (nenhum circuito real deixa elite "farmar" evento de entrada), não escopo acidental. **(b) seleção por prioridade de ranking** (`WorldTourLifecycle.js`, `applyOpenTierEntryPriority`) — antes, oversubscrição (mais candidatos que vagas) era resolvida pelo MESMO `pairScore` (dominado por overall_rating) que depois decide quem vence a chave; agora, uma função nova decide QUEM ENTRA (por rank) antes de `pairScore`/`ordered` decidirem QUEM VENCE (inalterado) — a mudança é só na porta, nunca no resultado do jogo. **(c) piso por prioridade** (achado #30, opção "a" já proposta) — 75% das vagas oversubscritas por ranking; 25% reservados (`OPEN_TIER_RESERVED_SHARE`, ponto de partida) pras duplas com MENOS `tournaments_played` na temporada (usando o campo já limpo pelo item 0) — o piso que evita exclusão permanente sem depender de sorteio. <br><br>**Medição antes/depois, mesma seed/escala/2-temporadas — a razão que definiu o item 1 da Fase 5 inteira desaparece**: demanda/capacidade Bronze+Silver, temporada 1: **9,99× → 1,57×**; regime (temporada 2): **15,29× → 0,75× — abaixo da capacidade existente.** Duplas nunca escaladas: temp.1 56,7%→35,8%; regime 67,5%→**16,9%**. **Isto muda a resposta à pergunta original da Fase 5** ("dimensionar a base do circuito", achado #32): com a seleção corrigida, os 40 eventos/ano ATUAIS deixam de estar subdimensionados — sobra capacidade em regime. Ampliar o calendário, com a agenda de tier em vigor, deixaria de ser necessário — "o item 3 responde o item 1", exatamente como antecipado antes de implementar. Classificação de títulos por tier (2 temporadas, `tournament-results.csv`) — **efeito limpo, exatamente localizado**: Bronze 95,8%→**29,2%** real, Silver 100%→**28,1%** real; Gold/Platinum/Masters/Elite/Crown **idênticos, tier a tier, dentro do ruído** (nenhum tocado). Agregado (159 torneios): 95,0%→59,7% real — não é enfraquecimento geral, é remoção dos reais de dois tiers específicos que, por desenho, nunca deveriam disputar (responde o item 3 original da Fase 5: "não é fazer os reais vencerem, é fazê-los aparecer nos lugares certos" — Gold pra cima seguem tão dominados quanto antes). Participação: bots mediana 0→**2** (mais da metade passa a jogar algo, não mais zero); reais média 40,83→26,46 (perderam o acesso fácil de Bronze/Silver, dependem só de Gold pra cima, tiers com menos vagas agregadas). Efeito colateral honesto, não escondido: reais SEM JOGAR NUMA temporada específica sobe (9,14→14,15) — mas a EXCLUSÃO PERMANENTE (interseção across temporadas) cai (27,8%→**16%** da união) — aparecem menos por temporada, mas rotacionam mais, prendem menos sempre os mesmos. Chaves incompletas: 0/80→0/80, 0/79→0/79 — nenhuma mudança, piso e preenchimento de reserva seguem fechando toda chave. <br><br>**Item 2 — portão de parceria implementado (desenho de 3 faixas aprovado na Fase 5).** `calculatePartnershipInterest` (`teamCompatibility.js:67`) — `available` corrigido NA FONTE pra também exigir `score >= T_low` (30, não só `career_status`); novo campo `friction` (`score < T_high`, 55) marca a faixa intermediária. Corrigir na fonte (não em cada consumidor) significa que os 3 caminhos de contratação, o inventário disponível/bloqueado do PartnerHub e a tela de comparação de ofertas herdam o portão automaticamente. **Três caminhos unificados** via `getSuggestedPartnerTerms` (a mesma função gap-based que o onboarding já usava): convite direto (`PartnerHub.jsx:handleInvite`, antes fixo 60/50) e oferta pré-formatada (`buildInitialPartnerOffers`, antes `[60,45,90,60][index]`/`prizeSplit:50`) — os dois caminhos que ignoravam o gap — agora usam a mesma função; endurece sozinho com o gap, sem precisar de ramo separado pra "faixa de fricção". `getAvailablePartners`/`getLockedPartners` (`career.js`) — o gate por XP pré-existente continua valendo (eixo diferente); candidatos dentro do nível mas com `score` baixo migram pra "bloqueado", reaproveitando a UI que já existia, com rótulo que distingue os dois motivos. `generatePartnerProposals`/`accept_partnership` — reconfirmado código morto (achado #31), não é um quarto caminho ativo. **Item 2.5 (ciclo de vida das ofertas) — achado novo: já estava resolvido.** Existe `processSpontaneousPartnerMarket` (`partnerOffers.js:120`, "Fase 15", anterior a esta auditoria), já ligada ao laço diário de produção (`gameStateLifecycle.js:97`), que expira ofertas pendentes e sorteia novas com chance real mensal (~22% livre/~9% pareado), reusando `buildInitialPartnerOffers` — herda o portão corrigido automaticamente. Não era uma lacuna a fechar, só não tinha sido conferida antes de propor o desenho. **Item 2.6 (efeito medido)** — contra o catálogo real do PartnerHub (340 candidatos), 6 estágios de carreira: Dia 1 (#1000/reputação 10) — Top 20 mundial **0/20 disponível** (invisível, não só difícil); Início (#500/rep.20) — Top 20 disponível mas **0/20 aberto** (só fricção); Meio 1 (#200/rep.40) — **12/20 já aberto**; Meio 2 em diante (#100+/rep.55+) — Top 20 totalmente aberto. Bate com o alvo declarado sem precisar de reajuste de `T_low`/`T_high`. <br><br>**Item 3 (menor) — as 3 falhas de suíte, datadas e corrigidas, não só descritas como "pré-existentes".** `git log`/`git blame`: as três nasceram no MESMO commit (`c012097`, 2 de setembro de 2026, "v95" — extração de `fnv1aHash` pra `src/lib/hashUtils.js`, parte desta auditoria). `test:sports-economy` — o script copia `sportsEconomyV26.js` pra um temp em `scripts/` mas só reescrevia o import de `localGame`, não o novo `import ... from './hashUtils.js'` (virava `scripts/hashUtils.js`, que nunca existiu); corrigido, reescreve os dois. `test:match-integrity`/`test:beta` (compõe o primeiro) — `random.js`/`PersonalityModel.js` passaram a importar via `@/lib/hashUtils.js` (alias só Vite resolve), quebrando o teste (`node` puro); corrigido na CLASSE do problema, não só no arquivo — `test-match-integrity.mjs` reescrito pro mesmo padrão `vite.ssrLoadModule` já usado por `audit-real-athletes-simulation.mjs`, em vez de import ESM direto (337 arquivos em `src/` usam `@/` — qualquer um alcançado no futuro pelo motor de partida quebraria o import direto de novo). Confirmado: as três, isoladas, verdes depois da correção. | **Verificação confirmou contaminação real em `tournaments_played` (campo separado, nenhuma conclusão qualitativa invertida — a de domínio real fica MAIS forte); agenda de tier implementada e medida — a razão de regime que motivou a Fase 5 inteira (15,3×) caiu pra 0,75× (abaixo da capacidade), exclusão real 67,5%→16,9%, efeito limpo e localizado em Bronze/Silver (Gold+ idêntico), reais deixam de "aparecer no lugar errado" exatamente como o item 3 original da Fase 5 pedia; portão de parceria implementado e calibração confirmada contra o catálogo real sem precisar de reajuste; item 2.5 era um achado, não uma lacuna — já resolvido antes desta fase; 3 falhas de suíte datadas ao commit exato e corrigidas na causa** |

---

## Lista priorizada de correções propostas (não implementadas — só avaliação de risco/impacto)

1. **[Alto impacto / Baixo risco] Popular `ai_partner_id` para os 24 reais a partir das 12 duplas do seed**, no bootstrap (`saveFoundation.js`), espelhando a intenção já registrada em `worldSeed2025.json.teams`. Resolve o #4 e o #14 na raiz, sem tocar em nenhuma fórmula de simulação — só inicialização de dado. Precisa decidir uma política de dissolução consistente com `aiPartnershipLifecycle.js` (duplas históricas deveriam ter contrato "mais estável" que pares aleatórios, ou usar a mesma regra?).
2. **[Alto impacto / Baixo risco] Corrigir a chave de `TeamRanking` para duplas com `ai_partner_id`** apontando para a linha canônica do seed em vez de criar uma chave derivada de ids ordenados — resolve o #5 sem mudar a lógica de pontuação.
3. **[Médio impacto / Baixo risco] Proteger a dupla histórica de dissolução automática** por um tempo maior, ou dar prioridade a ela no sorteio mensal quando ambos os membros estiverem livres — trata a causa da diluição por escala vista na Fase 2 (resultado B vs. A), sem impedir dissolução narrativa legítima no longo prazo.
4. **[Médio impacto / Médio risco] Dar aos reais um `birth_date` real (derivável da `age` do seed + `career_start_date`) e ligar `AthleteProfile` ao mesmo ciclo de envelhecimento/aposentadoria já usado em outros sistemas** — resolve o #13, mas precisa de um plano de substituição narrativa quando um real se aposentar (a pirâmide não pode ficar sem topo).
5. **[Médio impacto / Médio risco] Unificar (ou ao menos cross-referenciar por um id comum) o pool de ranking e o catálogo de adversários do jogador** — maior mudança arquitetural da lista; resolve a fragmentação de identidade (#1) mas toca em `career.js`, `bots.js`, `athleteCatalog.js` e todo consumidor de `BOTS_BY_DIFFICULTY`. Recomendo tratar como projeto à parte, não como hotfix.
6. **[Baixo impacto / Baixo risco] Remover ou efetivamente usar os campos mortos** (`seed_source` como flag real, `is_development_tournament`) — não muda comportamento, só reduz a distância entre o que os dados "dizem" e o que o código faz.
7. **[Baixo impacto / Baixo risco] Aumentar a proporção de reais no pool "Lenda" do catálogo do jogador**, ou dar peso extra a nomes reais no sorteio de `generateTournamentOpponent` nas rodadas decisivas de Elite/Crown — reforça o objetivo de design "enfrentar um real na final de um Major deveria ser a norma, não a exceção" sem exigir a unificação completa do item 5.
8. **[Alto impacto / Baixo risco] Corrigir o nome do campo em `normalizeAthlete` (`WorldTourLifecycle.js:77`) de `ranking` para `rank`** (ou ajustar `EntryManager.js` para aceitar `ranking` como alias, no mesmo espírito de tolerância a nomes legados já usado em `padel.js`) — reativa a elegibilidade por tier já implementada e correta em `EntryManager.js`, sem tocar em nenhuma regra de negócio nova. É a correção de menor risco/maior impacto desta lista inteira: uma mudança de 1 linha faz a hierarquia Silver→Crown voltar a significar algo na simulação de fundo.
9. **[Médio impacto / Baixo risco] Reduzir a severidade do "cold start" do item 15** — por exemplo, garantir que cada atleta real receba pelo menos uma chance de entrar no sorteio de parceria nos primeiros 1-2 meses de uma carreira nova, em vez de competir em pé de igualdade estatística com ~970 bots por uma vaga entre 8. Isso ataca diretamente o momento em que o jogador mais nota o problema (ano 1), complementando a correção estrutural do item 1.

**Ordem recomendada:** **8** (a correção de menor risco e maior alavancagem — 1 linha, reativa um sistema inteiro já pronto) → **1** → **2** → **9** → **3** → **6**. Deixando os itens 4 e 5 para discussão de escopo à parte dado o tamanho do impacto narrativo/arquitetural.
